# Project Radar 实施计划

> **执行记录：** V0 已在隔离 worktree 中按测试驱动完成；下列勾选项代表代码和自动化验证完成，不代表真实模型人工演示完成。  
> 文档状态：维护中  
> 负责人：bwz  
> 最近更新：2026-07-13  
> 权威范围：Project Radar 工程任务、依赖、进度与完成定义

[返回文档总览](./README.md)

**Goal：** 在现有 AgentV3 中完成“QA Event → Item / Project State → Radar → Agent 执行 → 状态回流”的可演示 V0 闭环。

**Architecture：** 主 Agent 写入 `end_stream` 后异步运行 Post-run Organizer，先持久化 Event 并更新 Item / Project，再由 Policy Gate 决定是否生成 Radar。前端通过 Overview、纠正接口和有限轮询展示状态，接受 Radar 后复用现有 `/api/query` 与 SSE 执行 AgentV3。

**Tech Stack：** React 18、Vite、FastAPI、Python、SQLite、AgentV3。

## 1. 全局约束

- 只修改当前用户有权访问的 Project、Item、Event、Session 和 Radar 数据。
- Post-run 后台失败不得延迟或改变主 Agent 的 `end_stream`。
- 每个 `source_run_id` 只能创建一个 Event。
- 一个 Event 最多归属一个 Item；Item 可以不属于 Project。
- Re-entry 与 Session End 不触发 V0 模型整理或 Radar 生成。
- Radar Action Run 更新 Event 与状态，但不递归生成 Radar。
- V0 不增加手工创建、删除、合并、批量整理和夜间重组。

---

## 2. 实施概览

V0 实施原则：

- Post-run 是 Event 整理和 Radar 生成的唯一边界；主 Agent 写入 `end_stream` 后异步执行。
- 每个正常完成的 QA 只生成一个 Event，整理失败仍保留在待分类。
- Project、Tracked Item 和 Activity Event 使用真实 SQLite 持久化，不再依赖人工 `session_metadata` 项目归属。
- 默认创建独立 Item；只有明确多事项目标才创建 Project。
- Re-entry 只读取已有 Overview 与 Radar，不运行模型。
- Radar Action Run 更新 Event 和状态，但不递归生成 Radar。
- 右侧工作区使用 `Todolist / Radar` 双 Tab；Radar 可打开居中双栏弹窗，V0 不做完整项目管理。
- Context Bundle 必须在服务端构造；所有 API 只支持登录用户。

### 2.1 当前进度

| 阶段 | 状态 | 交付物 |
|---|---|---|
| 产品定义 | 已完成 | 产品概要、零版产品需求已批准 |
| 架构与数据模型 | 已完成 | Project / Item / Event、Post-run 与 Radar 已落入 SQLite 和服务层 |
| UI 方案 | 已选定 | 右侧 Todolist / Radar 双 Tab；Radar 可打开居中双栏弹窗 |
| 后端实现 | 已完成 | 状态存储、Organizer、接口、Post-run、Radar 和执行适配 |
| 前端实现 | 已完成 | 右侧 Todolist / Radar 双 Tab、Overview 弹窗、Action Bar、有限轮询和执行接入 |
| 自动化验收 | 已完成 | backend、Agent 上下文、前端状态测试与生产构建 |
| 人工验收 | 待执行 | 真实模型端到端与五分钟演示 |

### 2.2 文件规划

后端：

| 文件 | 责任 |
|---|---|
| `backend/database.py` | 创建 Project、Item、Event、Radar 和反馈表，提供版本化事务读写 |
| `backend/radar_service.py` | Post-run Organizer、状态补丁、Overview 与 Radar 策略 |
| `backend/radar_post_run.py` | 严格模型适配、Post-run 调度、Organizer → Radar 编排 |
| `backend/radar_execution.py` | Radar 领取、服务端 Context Bundle 和终态转换 |
| `backend/server.py` | 注册 Overview / 纠正 / Radar API，并调度 Post-run |
| `backend/test_radar_database.py` | 持久化、幂等、版本和状态测试 |
| `backend/test_radar_service.py` | 自动整理、状态聚合、Radar 和上下文测试 |
| `backend/test_radar_api.py` | Overview、纠正、接受和反馈接口测试 |
| `backend/test_radar_post_run.py` | 异步调度、失败降级和防递归测试 |
| `agents/agentv3/core/context_pipeline.py` | 注入隐藏 Radar Context Bundle |

前端：

| 文件 | 责任 |
|---|---|
| `frontend/src/App.jsx` | Overview、Radar、弹窗和有限轮询状态 |
| `frontend/src/components/WorkspaceSidePanel.jsx` | 右侧 Todolist / Radar 双 Tab 容器 |
| `frontend/src/components/RadarPanel.jsx` | 单条 Radar 建议、反馈与双栏弹窗入口 |
| `frontend/src/components/ProjectRadarModal.jsx` | 居中双栏 Project / Item / Event 弹窗 |
| `frontend/src/components/RadarActionBar.jsx` | Action Bar 以及同文件内的详情和反馈面板 |
| `frontend/src/components/radarDisplayState.js` | 集中计算展示条件 |
| `frontend/src/radarApi.js` | Overview、Event、纠正、Radar 和轮询接口 |
| `frontend/src/radarExecution.js` | 接受结果校验、同 Session 执行参数和 single-flight 锁 |

## 3. 数据与整理基础

### 3.1 状态持久化

**依赖：** 无。  
**交付：** SQLite 可以保存和查询 Project、Item、Event、Radar 与反馈。

- [x] 先为表初始化、Event 幂等、Project / Item 版本更新和关系查询编写失败测试。
- [x] 在 `backend/database.py` 创建 `projects`、`tracked_items`、`activity_events`、`radar_items` 和 `radar_feedback`。
- [x] 为 `activity_events.source_run_id` 建唯一索引，重复调度返回原 Event。
- [x] 实现按用户查询活跃 Project、独立 Item、Non Project 和待分类的函数。
- [x] 实现带 `version` 的 Project / Item 条件更新和事务提交。
- [x] 实现 Event 创建、归属纠正和来源 Session 查询。
- [x] 实现 Radar 状态、去重、运行关联、到期恢复、新鲜度过滤和反馈读写。

验证：

```bash
uv run python -m pytest backend/test_radar_database.py -q
```

### 3.2 Post-run Organizer

**依赖：** 3.1。  
**交付：** 给定一个完整 QA，可以创建 Event，并更新或创建 Item / Project。

- [x] 先覆盖归入已有 Item、创建独立 Item、创建 Project、Non Project、待分类、完成和重新打开。
- [x] 在 `backend/radar_service.py` 实现 Event 壳记录与严格结构输出校验。
- [x] 加载当前 Session 最近 Item、活跃状态和相关候选，不把全部历史交给模型。
- [x] 一次分析输出 Event 摘要、归属、新建决定和状态补丁。
- [x] 整理成功后事务写入归属、Item State 与 Project State。
- [x] 整理失败时只更新 Event 为 `classification_failed`。
- [x] 用户纠正 Event 后重新计算受影响 Item 和 Project。
- [x] Project 进度只统计必要 Item；可选 Item 不阻止完成。

验证：

```bash
uv run python -m pytest backend/test_radar_database.py backend/test_radar_service.py -q
```

## 4. Overview、纠正与 Radar

### 4.1 Overview 与纠正 API

**依赖：** 3.1、3.2。  
**交付：** 前端可以读取状态并纠正自动整理结果。

- [x] 先覆盖未登录、越权、Overview、Event 时间线、错误目标和版本冲突测试。
- [x] 实现 `GET /api/radar/overview`。
- [x] 实现按 Item 查询近期 Event 与来源 Session。
- [x] 实现 Event 重新归入 Item、改为 Non Project 或待分类。
- [x] 实现 Item 移入或移出 Project、必要/可选、完成和重新打开。
- [x] 不提供手工创建、删除、合并和批量整理 API。
- [x] 所有读写按当前用户过滤。

验证：

```bash
uv run python -m pytest backend/test_radar_api.py -q
```

### 4.2 居中双栏弹窗

**依赖：** 4.1。  
**交付：** 右侧 Radar Tab 可以查看建议，并打开双栏弹窗查看和纠正 Project、Item 与 Event。

- [x] 以 `WorkspaceSidePanel` 统一承载 Todolist 与 Radar，移除聊天区中的临时胶囊入口。
- [x] Radar Tab 展示待处理数量、单条建议、推进、稍后、忽略和“查看全部项目”。
- [x] 新增 `ProjectRadarModal` 与样式，左侧展示 Project、独立 Item、Non Project、待分类。
- [x] 右侧展示状态、必要/可选、目标、完成标准、进展和近期 Event。
- [x] Event 展示一句话摘要、时间和来源 Session。
- [x] 接入纠正、移动、完成与重新打开操作，并处理版本冲突。
- [x] 确保无数据和接口失败时有明确空状态，不影响聊天。
- [x] 不渲染手工创建、删除、合并和批量管理入口。

验证：

```bash
npm --prefix frontend run build
```

### 4.3 Radar 生成与展示

**依赖：** 3.2、4.1。  
**交付：** 状态更新后可以生成零条或一条可展示 Radar。

- [x] 先覆盖无缺口、待分类、重复建议、错误 Session、缺少证据和合法建议测试。
- [x] 状态事务成功后才运行 Radar Candidate 校验与 Policy Gate。
- [x] Radar 必须关联 Tracked Item，并选择明确 `target_session_id`。
- [x] 实现查询、接受和反馈 API，以及最小新鲜度、稍后恢复和去重。
- [x] 新增 `RadarActionBar`、详情面板和集中展示条件。
- [x] 前端收到 `end_stream` 后立即查询一次，再按有限退避最多查询五次；Re-entry 只查询一次。
- [x] 同时最多展示一条 Radar，流式输出、等待回答和输入期间不出现。

验证：

```bash
uv run python -m pytest backend/test_radar_service.py backend/test_radar_api.py -q
node --test frontend/src/components/radarDisplayState.test.js
node --test frontend/src/radarApi.test.js
npm --prefix frontend run build
```

## 5. Agent 执行与 Post-run 闭环

**依赖：** 第 3、4 节。  
**交付：** 接受建议后真实运行 AgentV3，产出结果形成新 Event 并更新状态。

- [x] 接受接口使用条件更新 `pending → accepted`，只返回短消息、目标 Session 与 `radar_item_id`。
- [x] 前端携带 `radar_item_id` 调用现有 `/api/query` 并消费 SSE。
- [x] `/api/query` 更新 `running`、写入 `execution_run_id`，并在 `case` 中携带 Radar 标识。
- [x] 服务端从 Project、Item、Event 和目标 Session 构造隐藏 Context Bundle。
- [x] Context Bundle 参与 Prompt，但不显示为普通用户消息。
- [x] Worker 正常结束时 Radar `running → done`，异常或终止时进入 `failed`。
- [x] Radar Action Run 结束后创建 Event 并更新 Item / Project，但抑制新的 Radar Candidate。
- [x] 在主 Agent `end_stream` 后调度 Post-run，失败只记录 Event 状态和日志。
- [x] Session End 和 Re-entry 不调用整理或生成逻辑。

验证：

```bash
uv run python -m pytest backend/test_radar_post_run.py backend/test_radar_service.py backend/test_radar_api.py agents/agentv3/tests/test_context_pipeline.py -q
npm --prefix frontend run build
```

## 6. 里程碑与完成定义

| 里程碑 | 包含任务 | 完成标志 |
|---|---|---|
| 一：可整理 | 第 3 节 | 每个 QA 形成 Event，并能跨 Session 更新 Item / Project |
| 二：可查看纠正 | 4.1、4.2 | 居中双栏弹窗可以查看来源和纠正归属 |
| 三：可主动推进 | 4.3 | 状态更新后可以安静或展示一条 Radar |
| 四：可闭环 | 第 5 节 | AgentV3 真实产出形成新 Event，状态和反馈闭环 |

完成定义：

- 《零版产品需求》的 V0 验收标准全部满足。
- Event 幂等、状态聚合、纠正和重新打开测试通过。
- Post-run 异步整理、Radar 防递归和隐藏上下文测试通过。
- Overview、权限、纠正、Radar 和反馈接口测试通过。
- 前端弹窗、展示规则、有限轮询和生产构建通过。
- 五分钟 Demo 可以连续运行两次；兜底数据与真实能力边界说明准确。
- README、实施计划和测试文档更新为真实工程状态。

当前判定：代码任务、自动化验证和文档同步已完成；由于真实模型端到端与连续两次五分钟 Demo 尚未执行，V0 的“可合并实现”已具备，但“最终人工验收”仍未完成。详细测试数字和已知基线失败见《测试与演示》1.1。

## 7. 交互续跑的 Post-run 补漏修复

**问题：** 真实用户 QA 如果先进入 `waiting_user_input`，首次 Run 因回答未完成而不执行 Post-run；Todolist 或 ask_user Resume 又被 `control_path="resume"` 排除，导致整轮 QA 没有 Event。`bbb` 的 Case 5 第二轮因此出现“主 Agent 与 Todolist 已完成，但 Project Radar Item 仍为 `in_progress`”。

**目标：** 一个真实用户输入无论经历多少次 Todolist/ask_user Resume，最终正常进入 `idle` 时都使用原始用户消息和最终 Assistant 消息执行且仅执行一次 Post-run。

**涉及文件：**

| 文件 | 修改责任 |
|---|---|
| `src/conversation_state_v3.py` | 定义并持久化 `runtime_control.pending_post_run_qa`，提供读取、设置和清除函数 |
| `agents/agentv3/core/context_pipeline.py` | 真实用户输入开始时创建稳定 QA 身份；Resume 时恢复原始身份 |
| `agents/agentv3/core/postprocess.py` | 等待态保留 pending QA；最终态产生原始 user id、最终 assistant id 和稳定 source run id |
| `backend/radar_post_run.py` | 允许携带完整 pending QA 的 Resume 进入 Post-run，继续拒绝 replay 与 re-entry |
| `backend/server.py` | 使用 `post_run_qa.source_run_id` 构造 payload，并在终止边界清理 pending QA |
| `tests/test_conversation_state_v3_post_run.py` | pending QA 默认值、持久化、覆盖和清理测试 |
| `agents/agentv3/tests/test_context_pipeline.py` | 正常输入创建身份、Resume 恢复原始身份测试 |
| `agents/agentv3/tests/test_postprocess_regressions.py` | waiting 保留、最终 Resume 消费和最终 Assistant 选择测试 |
| `backend/test_radar_post_run.py` | Resume eligibility、payload 原始输入和幂等回归测试 |

### 7.1 持久化 QA 身份

- [x] 新增 `pending_post_run_qa` 默认字段及设置、读取、清除函数。
- [x] 校验 `user_message_id`、`source_run_id` 非空，并支持 Conversation State 持久化。
- [x] 新增 5 项状态单元测试并通过。

### 7.2 真实输入创建、Resume 恢复

- [x] 真实输入创建稳定 QA 身份；Todolist/ask_user Resume 恢复原始身份，不把合成反馈当成新 QA。
- [x] `prepare_runtime_state` 返回原始 `post_run_user_message_id` 和稳定 `post_run_source_run_id`。
- [x] 没有 pending QA 的内部 Run 仍返回 `None`，不猜测历史消息。
- [x] 新增 3 项定向测试并通过；该测试文件原有 6 项无关基线失败保持不变。

### 7.3 最终回答消费同一 QA

- [x] `waiting_user_input` 时保留 pending QA，且不产生半成品 `post_run_qa`。
- [x] 最终 Resume 使用原始 User Record、最终 Assistant Record 和稳定 source run id，并清除 pending QA。
- [x] QA chunk 仍忽略合成 `todolist_feedback`，但不再阻断 Project Radar Post-run。

### 7.4 Server 调度与幂等

- [x] Resume 最终完成时允许进入 Post-run；waiting、replay、re-entry、终止和异常仍被排除。
- [x] Server 优先使用原始 `post_run_qa.source_run_id`，不使用 Resume 的新 run id。
- [x] 显式终止清理 pending QA；`end_stream` 仍先于异步 Post-run。
- [x] 现有数据库并发与重复调度测试确认同一 `source_run_id` 只产生一条 Activity Event。

### 7.5 完整验证

- [x] 修复定向回归 63 项全部通过。
- [x] Project Radar 数据库、服务和 API 回归 118 项全部通过。
- [x] 组合回归共 93 项通过，仍有 6 项修复前已存在的 context pipeline 基线失败，本次没有新增失败。
- [ ] 使用独立测试账号重新执行 Case 5 前两轮，验证第二轮即使触发 Todolist 自动确认，最终仍生成一条 `completion_confirmed` Event，Item 进入 `completed`。
- [ ] 检查首次 waiting 阶段没有半成品 Event，最终 Event 的来源仍是原始自然语言消息，而不是 `todolist_feedback`。
- [x] 本节已更新为当前真实实现与自动化测试结果。
