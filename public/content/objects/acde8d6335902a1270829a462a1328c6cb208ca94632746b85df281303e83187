# Progress Agent（持续推进智能体）实施计划

> 文档状态：维护中  
> 负责人：bwz  
> 最近更新：2026-07-15  
> 权威范围：当前工程状态、近期任务与阶段顺序

[返回文档总览](../README.md)

## 1. 当前状态

V0 主闭环已经可运行：

```text
完整 QA
→ Post-run 触发 Organizer
→ Event / Item / Project 状态更新
→ Radar 判断并展示建议
→ 用户授权 Agent 执行
→ 执行结果作为 Event 回流
```

当前阶段：**阶段三“Radar 判断可信”收尾，同时进行 V0.2 Resume Brief 浏览器复测。**

| 工作项 | 状态 | 当前结论 |
|---|---|---|
| Organizer 与状态底座 | 已完成 | Event、单一主要 Item、Project Patch、完成层级和跨 Session 状态已实现 |
| Project 完成守门 | 已完成，待浏览器复测 | Item 完成不再由代码自动推进 Project 完成；Project 语义状态由 Organizer 维护 |
| Radar 新鲜度 | 已完成 | 同 Session 新 Query 和跨 Session Item / Project 版本变化都会淘汰旧的 `pending/snoozed` 建议 |
| Radar 判断协议 | 已完成并通过 3.2 实测 | Analyzer 输出动作、覆盖来源、约束、依赖和可执行性；Item blocker 是权威红绿灯，Policy Gate 确定性决定推送或静默 |
| Radar 决策审计 | 已完成 | 每个进入判断的 Event 记录结果、原因及结构化判断依据 |
| 自动化验证 | 已完成 | Radar 后端 230 项通过，本次 Ruff 通过；前端 Radar 27 项与生产构建为上一轮通过结果 |
| 真实模型验证 | 3.2 已通过，3.3 为产品问题 | 3.3 的 Radar 判断已正确，但 Action Runner 的 Context Snapshot 丢失跨 Session 审批事实；本轮不改代码、不重启，继续其他 Case |
| Resume Brief | 已实现，待浏览器复测 | 只读快照、自然日门槛、一句话摘要、三项目折叠和 Project / Session 双入口已接入 |

## 2. 阶段三要完成什么

- [x] 保存来源 QA，并在 Analyzer 前后校验它仍是该 Session 最新完整 QA。
- [x] Radar 绑定生成时的 `source_item_version` 和 `source_project_version`。
- [x] Organizer 更新状态时，在同一事务内使旧版本的未接受 Radar 失效；`accepted/running` 不受影响。
- [x] 把“是否有推进机会”“主回答是否覆盖”“用户约束是否命中”“当前是否可执行”拆成独立字段。
- [x] 由服务端 Policy Gate 确定性生成 `actionable_gap`、`main_answer_covered`、`insufficient_evidence` 或 `not_actionable`。
- [x] 生成产物内文字和泛化邀约不再算主 Agent 直接覆盖；用户约束按候选动作范围判断。
- [x] Item blocker 作为唯一权威前置条件：存在 blocker 时只允许直接解除 blocker 的动作。
- [x] Item 无 blocker 且已识别出允许、未覆盖的机会时，不得因模型新增可选条件而静默；证据不足降级为带“待确认”字段的可编辑初稿。
- [x] `radar_decisions` 保存动作、覆盖来源、约束和可执行性，系统失败与产品静默分开记录。
- [x] 前端根据最新 QA 的 Radar 分析状态进行有界轮询；生成建议或静默决策落定后立即停止，Re-entry 只查询一次。
- [x] 完成状态版本、覆盖来源、约束范围和并发竞态的自动化测试。
- [ ] 用真实模型完成正例、安静反例和跨 Session 旧建议失效复测。

阶段三完成标准：三个有效正例连续命中，安静反例连续通过；不出现旧建议残留、生成产物误判覆盖、约束范围扩大或主 Agent 重复提醒。

## 3. 下一步顺序

1. 以 3.2 三 Session 分批报价版作为跨 Session 状态、Radar 定向和 Action Runner 的演示基线。
2. 3.3 已记录为跨 Session Context Bundle 产品问题；继续执行 3.4 的主动提醒正例，以及 3.5、3.6、3.7 的覆盖与约束反例。
3. 若失败，先按审计字段判断是机会提取、覆盖来源、约束、可执行性、Candidate 校验、状态版本还是展示时序问题，再决定是否改 Prompt 或代码。
4. 三个正例和安静反例稳定后冻结 V0 演示路径；Resume Brief 已进入 V0.2 浏览器复测。

## 4. 后续阶段

| 阶段 | 目标 | 开始条件 |
|---|---|---|
| 阶段四：Resume Brief | 用户隔天或周末返回时，基于已有状态说明上次停在哪里、当前进度和恢复入口 | 自动化验证已通过，待浏览器复测 |
| 阶段五：持续感知 | 时间扫描、停滞检测、夜间重组和多项目恢复 | Resume Brief 价值验证通过 |

当前不做：多 Item 单轮更新、复杂项目管理、夜间自动重组、外部系统监控和未经授权的自动执行。
