# Progress Agent（持续推进智能体）实施计划

> 文档状态：维护中  
> 负责人：bwz  
> 最近更新：2026-07-14  
> 权威范围：当前工程状态、近期任务与阶段顺序

[返回文档总览](./README.md)

## 1. 当前状态

Progress Agent V0 已跑通以下闭环：

```text
完整 QA
→ Post-run 触发 Organizer
→ Event / Item / Project 状态更新
→ Radar 判断并展示建议
→ 用户授权 Agent 执行
→ 执行结果作为 Event 回流
```

| 工作项 | 状态 | 结论 |
|---|---|---|
| 产品模型 | 已完成，待复核 | 已统一 Progress Agent 总产品、Radar 模块和四层能力模型 |
| V0 前后端 | 已完成 | 状态持久化、Organizer、Overview、纠正、Radar 和执行回流已经实现 |
| 自动化验证 | 已完成 | V0 后端、Agent 上下文、前端 Radar 测试和生产构建已验证 |
| 真实浏览器验证 | 已完成 | 六个 Case 已覆盖跨 Session、独立 Item、Non Project、纠正、完成重开和主动推进 |
| 状态可靠性 | 待修复 | Case 1、Case 4 暴露出聚合和历史重算问题 |
| Radar 判断质量 | 待优化 | 需要固定正反例评测集和更清晰的证据展示 |
| Resume Brief | 未开始 | 状态与 Radar 稳定后进入 V0.2 |
| 连续演示排练 | 待执行 | 需要形成一条稳定的五分钟演示路径并连续运行两次 |

当前阶段：**阶段二“状态底座可信”。**

## 2. 当前要做的事

按依赖顺序完成以下任务：

### 2.1 Item 全量重算

- Event 被新增、移动、纠正、完成或重新打开后，读取 Item 的全部有效 Event。
- 按时间顺序重新计算进展、决策、阻塞、完成和重开状态。
- 不能让最新 Event 覆盖或删除仍然有效的较早事实。
- 使用 Case 4 补充失败测试和回归测试。

完成标准：纠正 Event 后，两个受影响 Item 都能得到完整、可追溯的状态。

### 2.2 Project 聚合

- Item 更新后，重新聚合 Project 下全部有效 Item。
- 更新当前阶段、整体进展、风险和完成状态。
- 只有必要 Item 阻止 Project 完成；可选 Item 不影响完成判断。
- 使用 Case 1 补充失败测试和回归测试。

完成标准：Project 摘要始终包含必要 Item 的最新有效状态，不停留在较早进展。

### 2.3 状态可靠性验收

- 连续多条 Event 不丢失历史事实。
- Event 从 Item A 移到 Item B 后，A、B 及其 Project 都正确重算。
- Item 完成或重新打开后，Project 同步变化。
- 同一轨迹重复执行得到一致结果。
- 每个状态结论都能追溯到 Event 和来源 Session。

完成标准：Case 1 和 Case 4 的已知问题修复，新增回归测试通过，其他 V0 Case 不退化。

## 3. 后续顺序

| 阶段 | 目标 | 开始条件 |
|---|---|---|
| 阶段三：Radar 判断可信 | 建立应该提醒与必须安静的评测集，改善证据、重复抑制和建议价值 | 状态可靠性验收通过 |
| 阶段四：Resume Brief | 用户跨工作日或周末返回时，读取已有状态并提供恢复入口 | Radar 判断质量稳定 |
| 阶段五：持续感知 | 时间扫描、停滞检测、夜间重组和多项目恢复 | Resume Brief 价值验证通过 |

当前不做：新增 Radar 类型、复杂项目管理、夜间重组、外部系统监控和自动执行。先把状态底座修正确，再扩展主动能力。

## 4. 当前阶段完成标志

阶段二完成需要同时满足：

- Item 和 Project 可以从全部有效 Event 确定性重算。
- 人工纠正不会丢失事实或产生错误完成状态。
- Case 1、Case 4 复测通过。
- 状态相关自动化测试通过，其他 V0 Case 无新增回归。
- README、技术设计和测试文档更新为最终实现状态。
