# Project Radar 技术设计

> 文档状态：已批准  
> 负责人：bwz  
> 最近更新：2026-07-10  
> 权威范围：V0 架构、数据模型、接口、存储、状态流与 Agent 执行上下文

[返回文档总览](./README.md)

## 1. 设计概览

- 在现有 AgentV3 主链路之外增加异步 Radar Sidecar，不阻塞当前回复。
- 跨会话聚合项目信息，但严格控制执行会话。
- V0 同时支持预置数据和真实 Agent 执行。
- 让 Radar 的原因、证据、状态和反馈可追溯。
- 为 V1 的 Session End Scan、去重和偏好学习保留扩展字段。

### 1.1 总体架构

![Proactive Agent 三层工作架构](./images/主动代理工作架构.png)

*图 1：Proactive Agent 的目标工作架构。Post-run Scan 是 V0 唯一生成触发器；Re-entry 只恢复已有 Radar；Session End 是 V1 的预整理触发器。*

> 范围说明：这张图同时表达 V0 主链路和后续演进方向。图中的 `Project State` 是目标架构中的项目级持久状态；当前 V0 只定义了 `session_metadata`、`RadarItem.context_snapshot` 和 `radar_items`，尚未完成 `Project State` 的数据模型、更新规则与并发控制设计。因此，它目前不能被视为 V0 已具备的存储能力。

V0 包含五个逻辑组件：

1. **触发器**：仅在 Post-run 边界发起生成扫描；Re-entry 只读取和校验已有建议。
2. **Radar 服务**：校验项目归属，生成、排序和过滤 Radar Item。
3. **Radar 存储**：持久化建议、反馈和上下文快照。
4. **前端展示层**：渲染右侧入口、Action Bar 和详情。
5. **Agent 执行适配层**：接受建议后构造 Context Bundle，并调用现有 AgentV3。

主链路：

```text
用户消息
  → 主 Agent 完成
  → Post-run Scan
  → 生成并保存 Radar Item
  → 前端读取并展示
  → 用户接受
  → 构造 Context Bundle
  → AgentV3 执行
  → 更新状态与反馈
```

### 1.2 V0 项目归属

V0 不建设完整项目表，直接复用会话已有的 `session_metadata`。演示数据为两到三个会话写入：

```json
{
  "project_id": "proactive-agent-demo",
  "project_role": "main",
  "project_brief": "确定 Project Radar 的 V0 范围并完成可运行演示"
}
```

- `project_role` 只允许 `main` 或 `source`。
- 同一用户、同一 `project_id` 下只能有一个 `main` 会话。
- Post-run Scan 只处理带 `project_id` 的会话。
- 来源会话只能从当前用户具有相同 `project_id` 的会话中选择。
- 自动项目聚类和主会话识别留到 V1。

## 2. 触发与生成

### 2.1 Post-run Scan

Post-run Scan 是 V0 唯一的 Radar 生成入口。主 Agent 完成本轮回复或复杂任务后，异步触发一次扫描。普通问答和复杂 Agent Run 共用同一工程入口，但扫描深度可以根据运行元数据调整。

只有同时满足以下条件才调度扫描：

- Agent 正常结束，最终状态为 `idle`。
- 本轮不是用户终止、执行异常或 `waiting_user_input`。
- 当前会话存在 `project_id`。
- 本轮不是由 Radar Action 发起的 Agent Run。
- 当前没有同一 `dedupe_key` 的待处理或已完成 Item。

Radar Action 发起的运行在 `case` 中携带 `radar_item_id`，V0 对这类运行跳过 Post-run Scan，避免递归产生 Radar。

扫描判断：

- 当前会话是否属于已配置项目。
- 是否出现决策、收敛、交付、汇报或后续跟进信号。
- 是否存在可以由 Agent 产出的明确下一步。
- 是否已有相同类型的待处理建议。

### 2.2 Re-entry

用户重新进入产品或项目会话时，只读取已有的待处理 Radar Item，并执行最小新鲜度和目标会话校验。Re-entry 不调用扫描服务，不执行新的模型推理，也不生成 Radar Item。

### 2.3 Session End

Session End 不属于 V0 的 Radar 生成触发器。V1 可以复用现有 Session End 记忆更新时机，生成会话摘要、项目状态和候选 Radar；V0 只允许用预置数据说明这种未来体验。

### 2.4 生成策略

允许生成 Radar 的条件：

- 会话属于当前项目。
- 有明确任务推进信号。
- 没有未处理的同类建议。
- 不在冷却期。
- 建议包含明确动作和证据。

输出约束：

- 每次扫描最多生成一条建议。
- 必须解释 `why_now`。
- 至少有一条 `evidence_ref`。
- 不能只是会话摘要。
- 无法确定归属或动作时不生成。

扫描使用独立的严格结构输出：

```json
{
  "should_create": true,
  "title": "收敛 Proactive Agent V0 范围",
  "summary": "产品形态已明确，但第一版范围尚未锁定。",
  "why_now": "继续扩展形态会降低演示聚焦度。",
  "why_this_session": "当前会话包含最近的产品决策上下文。",
  "evidence_refs": [],
  "action_label": "让 Agent 生成 V0 范围",
  "action_prompt": "基于当前决策生成 V0 范围清单。",
  "expected_output": "plan",
  "dedupe_key": "proactive-demo:v0-scope"
}
```

- 使用当前 Agent 组件中的 `llm`，输入持久化状态快照、项目元数据、相关会话摘要和待办。
- `should_create=false`、结构校验失败、证据为空或动作为空时不创建 Item。
- 模型异常只记录日志，不影响主 Agent 和页面状态。
- V0 演示配置关闭 next questions，避免两个后台任务并发调用同一个 LLM；后续再提供独立 `radar_llm`。

### 2.5 异步交付

后端在主 Agent 写入 `end_stream` 后启动 `_schedule_radar_scan()`。前端收到 `end_stream` 后复用现有 deferred polling 模式：

1. 400ms 后调用 `GET /api/radar/items?session_id=<会话标识>&status=pending`。
2. 未查询到 Item 时，先等待 600ms，随后每 1200ms 重试。
3. 最多查询六次；仍为空则停止，页面保持不变。
4. Re-entry 只查询一次，不启动扫描。

扫描完成后不向已经结束的 SSE 继续写消息，也不阻塞主 Agent 的 `end_stream`。

## 3. 数据模型

```ts
type RadarItemV0 = {
  id: string; // Radar Item 的全局唯一标识，用于查询、接受、反馈和状态更新。
  user_id: number; // Item 所属用户，用于权限隔离；V0 只支持已登录用户。
  project_id: string; // Item 所属项目，来自目标会话的 session_metadata.project_id。
  source_session_ids: string[]; // 生成建议时参考的同项目会话标识列表。
  target_session_id: string; // Action Bar 展示和 Agent 执行的唯一目标会话。
  type: "risk" | "next_step" | "decision"; // 建议类型：风险、下一步或待决策事项。
  priority: "high" | "medium" | "low"; // 展示优先级；同一时刻只展示最高优先级 Item。
  title: string; // 面向用户展示的简短建议标题。
  summary: string; // 对建议内容和潜在影响的一句话概述。
  why_now: string; // 解释为什么当前任务边界是合适的提醒时机。
  why_this_session: string; // 解释为什么应在 target_session_id 对应会话展示和执行。
  evidence_refs: Array<{ // 支撑建议判断的可追溯证据列表，至少包含一项。
    type: "memory" | "session" | "todo" | "file"; // 证据来源类别。
    title: string; // 证据的用户可读名称。
    snippet: string; // 支撑当前判断的精简证据内容。
    source_id?: string; // 原始会话、记忆、待办或文件标识；来源不可定位时可为空。
  }>;
  action_label: string; // 主要操作按钮文案，例如“让 Agent 生成 V0 范围”。
  action_prompt: string; // 服务端传给 AgentV3 的具体执行指令，不直接展示完整内容。
  expected_output: "draft" | "checklist" | "plan" | "summary"; // 期望 Agent 产出的结果类型。
  context_snapshot: { // 生成 Item 时冻结的执行上下文，避免接受建议时依赖前端当前消息。
    project_brief: string; // 项目目标、背景和当前阶段摘要。
    current_session_context: string; // 目标会话最近一轮的关键上下文。
    related_session_summaries: string[]; // source_session_ids 对应的相关会话摘要。
    memory_context: string[]; // 与建议动作直接相关的记忆片段。
    todo_context: string[]; // 项目相关的待处理和近期完成事项。
    constraints: string[]; // V0 范围、用户偏好和禁止事项等执行约束。
  };
  status: // Item 当前生命周期状态，用于控制展示、执行和终态处理。
    | "pending"
    | "accepted"
    | "running"
    | "done"
    | "snoozed"
    | "ignored"
    | "failed";
  dedupe_key: string; // 同类建议的稳定去重键，防止重复生成或重复展示。
  execution_run_id?: string; // 接受后关联的 Agent Run 标识，用于准确回写 done 或 failed。
  snoozed_until?: string; // 用户选择“稍后”后的恢复展示时间，使用 ISO 8601 时间字符串。
  expires_at?: string; // Item 的过期时间；超过后不再展示或执行。
  last_error?: string; // 最近一次执行失败原因，成功或未执行时为空。
  created_at: string; // Item 创建时间，使用 ISO 8601 时间字符串。
  updated_at: string; // Item 最近一次内容或状态更新时间，使用 ISO 8601 时间字符串。
};
```

V0 固定在 `target_session_id` 执行，不支持 `new_session`、`view_only` 等目标模式；所有动作统一为运行 Agent，不保留多种 `action_type`。反馈写入独立表，不嵌入 Radar Item。

前端消费 `title`、`summary`、`why_now`、`why_this_session`、`evidence_refs`、`action_label` 和 `status`。`action_prompt` 与 `context_snapshot` 仅由服务端读取。

## 4. 接口与存储

### 4.1 Post-run Scan 内部调度

V0 不提供面向前端的扫描接口。`run_agent_worker` 在成功写入 `end_stream` 后调用内部 `_schedule_radar_scan()`；Re-entry 和普通前端请求不能触发扫描。

调度器接收 `conversation_id`、`user_id`、持久化状态快照、运行标识、项目元数据和 LLM。生成结果直接写入 SQLite。

### 4.2 查询 Radar

`GET /api/radar/items?session_id=<会话标识>&status=pending`

只返回当前用户有权访问、与目标项目匹配且可展示的建议。

### 4.3 接受建议

`POST /api/radar/items/{id}/accept`

服务端执行新鲜度检查，使用条件更新将状态从 `pending` 改为 `accepted`，但不直接启动 Agent。接口返回：

```json
{
  "radar_item_id": "radar-001",
  "target_session_id": "...",
  "visible_message": "Project Radar：请帮我生成 Proactive Agent V0 范围。"
}
```

### 4.4 执行建议

前端收到接受结果后，携带 `radar_item_id` 调用现有 `POST /api/query`，请求增加可选字段：

```json
{
  "conversation_id": "...",
  "query": "Project Radar：请帮我生成 Proactive Agent V0 范围。",
  "radar_item_id": "radar-001"
}
```

服务端验证 Item 属于当前用户、状态为 `accepted` 且目标会话匹配，然后：

1. 将 Item 更新为 `running` 并写入本轮 `execution_run_id`。
2. 从数据库加载 `action_prompt` 与 `context_snapshot`。
3. 以隐藏的 `functional_instruction` 注入 Context Bundle。
4. 复用现有 `/api/query`、Streamer 和 SSE 执行 AgentV3。
5. `case` 携带 `radar_item_id`，供 Worker 完成时更新 Item，并抑制本轮之后的 Post-run Scan。

### 4.5 提交反馈

`POST /api/radar/items/{id}/feedback`

请求包含反馈值和可选说明。接口更新 Radar 状态，并写入独立反馈记录。

### 4.6 存储设计

V0 使用现有 SQLite 基础设施，新增两张逻辑表：

**`radar_items`**

保存 V0 Radar Item。JSON 字段包括 `source_session_ids`、`evidence_refs` 和 `context_snapshot`；普通字段包括用户、项目、目标会话、文案、动作、状态、去重键、执行运行标识和时间字段。

**`radar_feedback`**

保存 Radar 标识、用户反馈、说明和创建时间。单次反馈不直接写入长期记忆。

索引至少包括：

- `(user_id, target_session_id, status)`：当前会话查询待处理 Item。
- `(user_id, project_id, dedupe_key, status)`：生成前去重。
- `(execution_run_id)`：Worker 完成时回写状态。

## 5. 执行上下文与状态控制

### 5.1 Context Bundle

点击推进时，后端重新构造上下文，而不是依赖前端已经加载的消息。

Context Bundle 包含：

- `user_request`：用户接受的 Radar 动作。
- `project_brief`：项目目标、背景和当前状态。
- `current_session_context`：当前会话最近的关键消息。
- `related_session_summaries`：来源会话的摘要，而不是完整消息。
- `memory_context`：与任务最相关的记忆。
- `todo_context`：项目相关的待处理和近期完成事项。
- `radar_context`：原因、证据和建议动作。
- `constraints`：用户偏好、V0 范围和禁止事项。

聊天区只显示一条短消息，例如：

```text
Project Radar：请帮我生成 Proactive Agent V0 范围。
```

完整 Context Bundle 由服务端转成 `input_kind="functional_instruction"`、`source="runtime"`、`hidden=true` 的输入记录传给 AgentV3。前端不能提交或覆盖该隐藏内容。

### 5.2 状态与并发控制

```text
pending → accepted → running → done
pending → snoozed
pending → ignored
running → failed
```

接受建议时必须使用条件更新，只有 `pending` 状态可以进入 `accepted`，避免重复点击启动多个 Agent Run。

执行状态与 Agent Run 绑定：

- `/api/query` 成功占用目标会话后：`accepted → running`，写入 `execution_run_id`。
- Worker 以 `idle` 正常结束：`running → done`。
- Worker 异常或用户终止：`running → failed`，写入 `last_error`。
- V0 的 Radar Action Prompt 明确要求直接生成目标产物，不调用 `ask_user`；若仍进入 `waiting_user_input`，将 Item 标记为 `failed`，避免无法关联后续 Resume。
- 只有 `case.radar_item_id` 对应的 Worker 可以更新该 Item；普通后续对话不能误改 Radar 状态。

执行异常时：

- 将状态更新为 `failed`。
- 保留失败原因和最近更新时间。
- 允许用户重新尝试，但不能并发重复执行。

### 5.3 新鲜度、去重与冷却

V0 最小新鲜度检查：

- Item 仍为 `pending`。
- 目标会话仍存在。
- 没有相同 `dedupe_key` 的已完成 Item。

以下情况在 V1 做完整重新校验：

- 目标会话出现新消息。
- 相关待办状态变化。
- 相关记忆被删除或更新。
- 上下文快照过期。

用户选择稍后后设置 `snoozed_until`，同一 Item 在到期前不展示；用户选择忽略后更新为 `ignored`。V0 依靠 `dedupe_key + status` 防止立即生成同类 Item，不实现复杂偏好降权。

## 6. 工程接入与实现边界

### 6.1 前端接入点

建议在现有结构上增量接入：

- `frontend/src/App.jsx`：维护 Radar 状态、查询数据、接受建议并复用现有消息发送流程。
- `frontend/src/components/TodoListPanel.jsx`：增加轻量 Radar 入口或扩展为右侧标签区域。
- 新增 `frontend/src/components/RadarActionBar.jsx`：负责输入框上方提示。
- 新增 `frontend/src/components/RadarDetailPanel.jsx`：负责原因、证据和操作。
- 新增 `frontend/src/radarApi.js`：封装查询、接受、反馈和 end-stream 有限轮询。

Action Bar 的显示判断应集中在单一选择函数中，避免条件散落在多个组件。

### 6.2 后端接入点

- `backend/server.py`：注册 Radar API，并在主 Agent 完成后调度 Post-run Scan。
- `backend/database.py`：创建 Radar 表及读写函数。
- 新增独立 Radar 服务模块：封装生成策略、排序、新鲜度和状态转换。
- 扩展现有 QueryRequest 与上下文管线以接收 `radar_item_id` 和隐藏 Context Bundle，不建立第二套 Agent 执行系统。

### 6.3 V0 真实能力与模拟边界

真实实现：

- Post-run 异步调度、结构化 LLM 扫描和有限轮询交付。
- Radar 查询、展示、接受、状态更新和反馈。
- SQLite 持久化。
- 至少一次真实 AgentV3 产出。

允许模拟：

- 在 `session_metadata` 中人工标记项目归属和来源会话。
- 扫描失败时用于演示兜底的一条高质量预置 Radar Item。
- 历史会话摘要和未来 Session End 预整理的展示数据。
- 复杂排序、偏好学习和完整失效计算。

### 6.4 安全与权限

- 所有 Radar 查询和操作必须按当前用户过滤。
- V0 Radar API 使用 `require_auth`，不向共享匿名用户开放。
- 不能读取或引用其他用户的会话、记忆和待办。
- 前端提交的 `project_id`、`session_id` 和状态不能直接作为可信结果。
- Context Bundle 只在服务端构造，避免客户端伪造隐藏上下文。
