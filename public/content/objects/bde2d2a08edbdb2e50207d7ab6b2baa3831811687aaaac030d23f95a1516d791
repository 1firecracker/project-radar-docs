# Case 5 完成重开与 Radar 执行实测

> 文档状态：已实测  
> 测试日期：2026-07-11，2026-07-13 补充核验  
> 测试用户：bwz  
> 测试分支：`codex/bwz`  
> 权威范围：明确完成、反例重开、Radar 主动建议、AgentV3 执行与 Event 回流

[返回文档总览](../README.md) · [返回演示案例](../演示案例.md)

## 1. 这次发生了什么

用户先说要上线一个客户反馈表单，并给出三条完成标准。后来用户明确表示三条标准都已验证，系统因此把事项标记为完成。

故事没有停在这里。用户在另一个 Session 里发现：手机端的提交按钮会被底部导航遮住，用户根本无法提交。系统没有死守之前的“已完成”，而是保留旧的完成证据，同时把事项重新打开。随后 Radar 主动提出“生成修复与验证清单”，用户接受后，AgentV3 真的执行了这一步，执行结果又变成一条新的 Event 回到原事项中。

最终结论：**Case 5 通过。完成、重开、主动建议、真实执行和结果回流均成功，执行后没有递归产生同类 Radar。**

## 2. 第 0 步：从空白环境开始

本次先清空测试用户的会话、Project Radar 数据和长期记忆，确认页面没有旧项目或旧建议。

![彻底清理后的空白状态](../images/Case5实测/00-彻底清理后的空白状态.jpg)

## 3. Session A：创建事项并明确完成

先新建 Session A。

![新建反馈表单 Session A](../images/Case5实测/01-新建反馈表单SessionA.jpg)

用户输入：

> 请持续跟踪一个独立事项：“上线客户反馈表单”。完成标准有三条：公开链接可以访问、必填字段可以成功提交、提交后显示确认提示。先帮我检查这三个完成标准是否足够明确。

![输入反馈表单完成标准](../images/Case5实测/02-输入反馈表单完成标准.jpg)

主 Agent 检查了三条标准，Post-run 随后创建独立 Item“上线客户反馈表单”。它没有为了单一事项创建 Project。

![反馈表单事项创建完成](../images/Case5实测/03-反馈表单事项创建完成.jpg)

接着，用户明确确认三条完成标准都已通过：

> 客户反馈表单的公开链接已经可以访问，必填字段提交成功，提交后也会显示确认提示。我已经逐项验证，请把“上线客户反馈表单”标记为完成。

![输入明确完成确认](../images/Case5实测/04-输入明确完成确认.jpg)

主 Agent 回答结束后，系统生成 `completion_confirmed` Event，并把 Item 更新为 `completed`。

![完成确认回答结束](../images/Case5实测/05-完成确认回答结束.jpg)

此时 Item 的时间线保留两条 Event：创建事项和用户明确确认完成。Radar 保持安静，没有继续催促已经完成的工作。

![事项完成状态与两条 Event](../images/Case5实测/06-事项完成状态与两条Event.jpg)

## 4. Session B：一个反例推翻完成结论

用户新建 Session B，让新证据来自另一段对话。

![新建反例 Session B](../images/Case5实测/07-新建反例SessionB.jpg)

用户输入：

> 刚发现一个反例：手机端打开客户反馈表单时，提交按钮会被底部导航遮住，用户实际上无法完成提交。所以这件事还不能算完成。请先记录问题和影响，不要现在写修复代码。

![输入移动端反例](../images/Case5实测/08-输入移动端反例.jpg)

主 Agent 只记录现象、影响和优先级，没有越过用户要求直接写修复代码。

![反例回答与事项重新打开](../images/Case5实测/09-反例回答与事项重新打开.jpg)

Post-run 把新 Event 归回原 Item，Event 类型为 `reopened`。Item 从 `completed` 回到 `in_progress`，旧的完成 Event 仍然留在时间线里。

![事项从完成重新打开](../images/Case5实测/10-事项从完成重新打开.jpg)

这一步说明系统追踪的不是一句静态结论，而是“这件事现在到底有没有真正完成”。后续出现可靠反例时，状态可以被新事实推翻。

## 5. Radar 主动推进并真实执行

反例整理完成后，Radar 生成建议“制定移动端提交遮挡修复与真机验证计划”，主要动作是“生成修复与验证清单”，目标 Session 正是发现问题的 Session B。

用户接受建议后，聊天中留下“执行：生成修复与验证清单”的动作记录。AgentV3 随后输出底部安全区适配、按钮定位、底部占位、层级排查和键盘场景等修复方向，并给出真机验证步骤与三条完成标准的移动端验收口径。

![Radar 执行结果与无递归建议](../images/Case5实测/11-Radar执行结果与无递归建议.jpg)

这张图同时展示了执行后的 Radar 状态：右侧显示“暂无需要推进的建议”。也就是说，Radar Action Run 的结果会参与状态更新，但不会再次触发同类建议形成无限循环。

## 6. 执行结果回到原事项

执行完成后，系统新增第 4 条 Event：

> 生成移动端提交按钮遮挡问题的修复方向与真机验证清单（仅计划，不含代码）

Item 的进展随之更新为“已生成修复方向与真机验证步骤，但尚未实现修复代码，问题未解决”，状态仍为 `in_progress`。Radar 的执行结果没有错误地把事项重新标记为完成。

![执行结果回流与四条 Event](../images/Case5实测/12-执行结果回流与四条Event.jpg)

最终时间线如下：

```text
上线客户反馈表单（in_progress）
├── Event 1：创建事项并检查完成标准（Session A / progress）
├── Event 2：用户逐项确认完成（Session A / completion_confirmed）
├── Event 3：发现手机端阻断性反例（Session B / reopened）
└── Event 4：Radar 执行结果回流（Session B / progress）
```

## 7. 实测判定

| 检查项 | 实际结果 | 结论 |
|---|---|---|
| 单一事项是否保持独立 | 创建 Standalone Item，没有创建 Project | 通过 |
| 用户明确确认后能否完成 | Item 进入 `completed`，生成 `completion_confirmed` Event | 通过 |
| 新反例能否重新打开 | Item 回到 `in_progress`，生成 `reopened` Event | 通过 |
| 原完成证据是否保留 | 完成 Event 仍在时间线 | 通过 |
| Radar 是否命中真实缺口 | 建议生成移动端修复与验证清单 | 通过 |
| Radar 是否在正确 Session 执行 | 目标和执行均落在 Session B | 通过 |
| 是否真实调用 AgentV3 | 生成了完整修复方向和真机验证清单 | 通过 |
| 执行结果是否回流 | 新增第 4 条 `progress` Event | 通过 |
| 执行后是否误判完成 | Item 仍为 `in_progress` | 通过 |
| 是否递归产生同类 Radar | 执行后显示暂无建议 | 通过 |

数据库最终状态也与界面一致：1 个 Standalone Item、4 条已分类 Event、1 条状态为 `done` 的 Radar；Item 版本为 4，`completed_at` 已在重新打开后清除。

## 8. 仍需注意的展示问题

Radar 建议卡是待处理态界面。建议一旦被接受并执行完成，右侧只展示“暂无需要推进的建议”，当前产品没有历史 Radar 列表。因此事后可以从聊天中的“执行：生成修复与验证清单”、Agent 输出、Item 回流 Event 和数据库 `done` 状态证明闭环，但不能在界面中重新打开那张已经完成的建议卡。

这不影响本 Case 的功能判定，但后续如果要增强演示和可追溯性，可以增加“历史建议”入口，展示已接受、已忽略和已完成的 Radar。

## 9. 附录：复现输入清单

本 Case 使用两个 Session。每一句发送后，等待主 Agent 回答结束和 Post-run 整理完成；第 3 轮后还要等待 Radar 建议出现。

### Session A

**第 1 轮：创建带完成标准的事项**

> 请持续跟踪一个独立事项：“上线客户反馈表单”。完成标准有三条：公开链接可以访问、必填字段可以成功提交、提交后显示确认提示。先帮我检查这三个完成标准是否足够明确。

**第 2 轮：明确确认事项完成**

> 客户反馈表单的公开链接已经可以访问，必填字段提交成功，提交后也会显示确认提示。我已经逐项验证，请把“上线客户反馈表单”标记为完成。

### Session B

新建一个对话，再发送下面这句话。

**第 1 轮：提供推翻完成状态的反例**

> 刚发现一个反例：手机端打开客户反馈表单时，提交按钮会被底部导航遮住，用户实际上无法完成提交。所以这件事还不能算完成。请先记录问题和影响，不要现在写修复代码。

Post-run 完成后，切换到右侧 Radar。若出现“制定移动端提交遮挡修复与真机验证计划”或含义等价的建议，点击主要动作“生成修复与验证清单”。这是 Radar 按钮操作，不需要手工输入新的聊天消息。等待 AgentV3 执行结束后，再到“查看全部项目”中确认第 4 条 Event 回流。
