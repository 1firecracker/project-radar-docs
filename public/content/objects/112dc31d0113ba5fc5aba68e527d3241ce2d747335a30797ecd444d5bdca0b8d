# Progress Agent（持续推进智能体）产品概要

> 文档状态：评审中  
> 负责人：bwz  
> 最近更新：2026-07-17  
> 权威范围：产品发心、能力模型、核心概念、使用时机、产品形态与演进路线

[返回文档总览](../README.md)

## 1. 产品发心与定位

Progress Agent 是 AgentV3 中负责持续推进的辅助智能体。它跨对话维护用户事项的真实进度，帮助用户恢复上下文、补齐当前判断，并在合适时机经用户授权继续推进。

它解决的不是“如何保存更多聊天记录”，而是三个连续性问题：

- **状态断裂**：跨 Session 信息散落，用户回来后需要重新梳理。
- **条件遗漏**：已记录的约束、依赖或风险没有进入当前判断。
- **推进中断**：主 Agent 只回答当前问题，错过状态变化后已经成熟的下一步。

Progress Agent 把聊天活动转化为可追踪状态：每个完整 QA 留下一条来源清晰的 Event；可持续推进的目标形成 Item；多个相关 Item 可以组成 Project；普通问答进入 Non Project。系统再基于这些状态回答三个问题：上次做到哪里、当前判断是否遗漏已记录的关键条件、现在是否出现用户尚未提出但已经成熟的下一步。

一句话概括：**记住状态，补齐判断，推动下一步。**

产品不替用户擅自执行，不在主 Agent 输出过程中插话，也不把所有对话都变成项目。用户可以查看证据、纠正归属、确认完成或重新打开事项，并决定是否让 Agent 继续推进。

## 2. 统一能力模型

Progress Agent 采用“触发时机—核心能力—状态底座—用户界面”四层模型：

| 层级 | 内容 | 回答的问题 |
|---|---|---|
| 触发时机 | Post-run、Re-entry、未来的 Session End、定时或外部变化 | 什么时候运行 |
| 核心能力 | Organizer、Resume Brief、Radar、Action Runner | 系统做什么 |
| 状态底座 | Event、Item、Project、Radar 与反馈 | 依据什么判断 |
| 用户界面 | 项目总览、恢复简报、Radar 卡片、执行结果 | 用户看到什么 |

四项核心能力的责任分别是：

| 能力 | 责任 |
|---|---|
| Organizer | 从完整 QA 提取 Event，判断归属，更新一个主要 Item，并维护所属 Project 的语义状态和当前阶段 |
| Resume Brief | 根据已有状态说明上次停在哪里、当前状态和恢复入口 |
| Radar | 发现当前判断遗漏的已记录关键条件，或用户尚未提出但已经成熟的高价值动作 |
| Action Runner | 在用户授权后构造上下文、调用 AgentV3，并把结果回写状态 |

Project Radar 保留为主动发现模块和界面名称，不再指代整个产品。Post-run、Re-entry 和 Session End 都只是触发时机，不与 Organizer、Radar 等能力处于同一层。

## 3. 使用时机与主链路

V0 的核心触发器是 Post-run：

```text
用户与主 Agent 完成一个 QA
→ Post-run 触发 Organizer
→ 创建 Event 并记录来源 Session
→ 归入或创建 Item / Project，或进入 Non Project / 待分类
→ Organizer 更新 Item State 与 Project 语义状态
→ 服务端统计 Item 进度并校验状态转换
→ Radar 检查尚未提出的关键缺口
→ 保持安静或生成一条 Radar Item
→ 用户查看、纠正或授权 Action Runner 推进
→ 执行结果形成新 Event 并回流状态
```

其他时机按版本区分：

- **Re-entry（V0）**：用户重新进入时只读取已有 Overview 和仍有效的 Radar，不执行 Organizer，也不创建新 Event。
- **Resume Brief（V0.2）**：用户跨工作日或周末返回时，根据已有状态提供只读进度简报；只有用户继续执行后才产生新 Event。
- **Session End、定时扫描和外部变化（V1 以后）**：用于停滞、期限、夜间重组和外部状态变化，不属于当前 V0。
- **普通对话进行中**：不实时整理、不突然展示新 Action Bar，避免与主 Agent 和用户输入争夺注意力。

Resume Brief 与 Radar 解决不同问题：前者回答“我上次做到哪里、从哪里恢复”，后者回答“有没有我尚未提出、但现在值得推进的动作”。两者共享同一状态底座，但不能混成同一种提醒。

## 4. 核心概念与产品形态

| 概念 | 含义 |
|---|---|
| Project | 可选的长期目标容器，由多个 Item 组成 |
| Item | 可以明确判断是否完成的事项；可以独立存在，也可以属于 Project |
| Event | 每个完整 QA 产生的不可丢失活动记录，包含一句话摘要和来源 Session |
| Non Project | 不需要持续推进的普通问答，可按生活、学习和其他分类 |
| 待分类 | 整理失败或无法可靠归属的 Event，等待纠正或后续重试 |
| Radar Item | 基于 Item / Project 状态生成的一条可解释、可执行主动建议 |
| Resume Brief | 根据已有状态生成的只读恢复简报，不代表发生了新工作事实 |

V0 保持 AgentV3 主聊天页结构：右侧工作区提供 `Todolist / Radar` 双 Tab；Radar Tab 展示当前最高价值建议及待处理数量；输入框上方按需出现一条 Action Bar。“查看全部项目”打开居中双栏弹窗，展示 Project、独立 Item、Non Project、待分类、近期 Event 与来源 Session。

![Radar V0 极简界面方案](../05-资源/图片/project-radar-main-page-minimal-v1.png)

*图 1：Progress Agent 中 Radar 模块的 V0 界面，不代表完整能力架构。*

V0 允许用户移动 Event、改为 Non Project、移动 Item、确认完成和重新打开；不提供完整项目管理器，也不支持手工创建、删除、合并和批量整理。

核心原则：

| 原则 | 要求 |
|---|---|
| 全量留痕 | 每个完整 QA 都形成 Event；整理失败不能导致记录丢失 |
| 单轮单事项（V0） | 一个完整 QA 只创建或更新一个主要 Item；同一 QA 推进多个 Item 留作后续扩展 |
| 状态可信 | Organizer 理解 Project 语义阶段；代码确定性汇总 Item 进度并阻止非法状态转换 |
| 目标导向 | Item 和 Project 的状态服务于“是否彻底完成” |
| 完成守门 | Item 完成不等于 Project 完成；项目级完成标准未满足时必须继续推进 |
| 主动推进 | Radar 必须提供明确动作，不能只是摘要、通知或重复主 Agent |
| 动作级判断 | 只判断同一个动作是否已覆盖或被约束；生成产物中的文字和泛化邀约不算直接覆盖 |
| 跨会话新鲜度 | Radar 依赖明确的 Item / Project 版本；任何 Session 更新状态后旧建议必须失效 |
| 低打扰 | 无高价值缺口时保持安静，不在主任务进行中插话 |
| 可解释可纠正 | 展示原因、证据、来源 Session，并允许用户修正 |
| 定向执行 | 可以跨 Session 理解状态，但只在明确匹配的 Session 中执行 |

## 5. 版本路线与当前重点

| 版本 | 目标 | 主要能力 |
|---|---|---|
| V0 稳定版 | 稳定已实现的主动推进闭环 | Post-run、Organizer、Project / Item / Event、单条 Radar、Action Runner、状态回流 |
| V0.1 | 让状态和主动判断可信 | 全量 Event 重算、Organizer 维护 Project 状态、完成守门、Radar 评测、重复与过期抑制 |
| V0.2 | 验证跨时间恢复价值 | 只读 Resume Brief、跨工作日或周末返回、继续入口 |
| V1 | 建立持续工作节奏 | 时间扫描、停滞检测、夜间重组、多项目恢复、多 Radar 管理 |
| V2 | 感知真实工作环境 | 外部工具变化、跨系统状态和持续监控 |

当前工程已经跑通 V0 前后端、六类真实浏览器 Case、Radar 执行与 Event 回流。历史 Event 重算、Organizer Project Patch、完成层级守门、跨 Session Radar 版本失效、动作级确定性 Policy Gate、Item blocker 就绪门禁和状态感知有界轮询均已实现。主动判断评测集 3.2 已固化为三 Session 分批报价版：甲、乙、丙报价分散写入同一 Item，最后一份报价回到已有 Session B 后触发选型表格草稿 Radar，并完成 Action Runner 与 Event 回流。旧版 3.3 因一个项目状态被拆成多个 completed Item 而未形成有效提醒，现已改为同一个“完成上线回滚方案”Item 跨 Session 逐步解除 blocker；下一步先重新验证 3.3，再继续 3.4—3.7，稳定后进入 V0.2 的 Resume Brief。

## 6. 验证目标与研究依据

Progress Agent 的阶段性验证目标是：

- 不同 Session 的 QA 能否稳定更新正确的 Item 或 Project。
- 用户能否理解并纠正自动整理结果，且纠正后不丢失历史事实。
- Item 和 Project 能否准确反映进展、阻塞、完成和重新打开。
- 已记录的约束、依赖、风险和完成标准能否进入当前判断，而不是由模型凭空补造流程或产物。
- Radar 是否能发现真正有价值、未被主 Agent 覆盖的推进机会。
- 用户是否愿意授权 Agent 执行，且产物能够减少下一步思考成本。
- 用户跨天返回时能否在短时间内恢复工作，而不必重读多个 Session。
- 整理、恢复和 Radar 是否足够低打扰。

当前 V0 的具体范围和验收口径以[零版产品需求](./零版产品需求.md)为准。总模型决策见[采用持续推进智能体总模型](../02-架构与设计/决策记录/采用持续推进智能体总模型.md)。

研究依据：

- [Microsoft Work Trend Index：数字债务与创新](https://www.microsoft.com/en-us/worklab/work-trend-index/will-ai-fix-work)
- [Asana：Work About Work](https://asana.com/resources/why-work-about-work-is-bad)
- [Atlassian：State of Teams 2026](https://www.atlassian.com/blog/state-of-teams-2026)
- [Slack Workforce Index：AI 输出信任问题](https://slack.com/blog/news/the-workforce-index-june-2024)
- [任务边界与合适打断时机研究](https://www.sciencedirect.com/science/article/abs/pii/S074756320500107X)
- [低负荷时机与任务打断研究](https://pmc.ncbi.nlm.nih.gov/articles/PMC11775001/)
