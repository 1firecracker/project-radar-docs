# Radar 主动判断评测集

> 文档状态：评审中  
> 负责人：bwz  
> 最近更新：2026-07-14  
> 权威范围：Radar 应该提醒与必须安静的固定评测 Case、判定口径和通过标准

[返回文档总览](./README.md)

## 1. 评测目标

本评测集只回答一个问题：**完整 QA 结束并完成状态整理后，Radar 是否发现了用户尚未提出、主 Agent 尚未覆盖、现在又确实值得执行的一步。**

它不把 Organizer 的基础归属准确率计入 Radar 得分，也不把摘要、记忆召回或普通下一步建议算作 Radar 价值。每次运行先验证状态前提：前提不成立记为“前置失败 / 本轮无效”，单独记录 Organizer 问题；只有状态前提成立后，才评价 Radar。

## 2. 统一判定口径

一条 Radar 只有同时满足以下条件才算正确：

1. **新价值**：不是用户本轮明确要求的动作，也不是主 Agent 最终回答已经指出的同一缺口。
2. **当前可推进**：前置条件已满足，动作现在可以由 Agent 执行；仍被阻塞时必须安静。
3. **状态相关**：关联明确的 Item 或 Project，不从 Non Project 或待分类内容生成。
4. **可执行**：动作能产生清单、计划、草稿或总结，不能只有通知、摘要或泛泛提醒。
5. **可解释**：至少一条证据可追溯到 Event，并指向明确的目标 Session。
6. **时机正确**：只在最新完整 QA 的 Post-run 后产生；流式回答、等待用户和 Re-entry 时不新生成。
7. **不过期**：新 Query 淘汰旧的 `pending/snoozed` 建议，但不打断已授权的 `accepted/running` 动作。
8. **不递归**：Radar Action Run 更新状态并产生 Event，但不再生成 Radar。

判定优先级：**先判断是否必须安静，再评价建议内容。** 不该出现却出现的建议，标题再好也算失败。

## 3. 主动判断质量 Case

统一规则：文中的 `T` 是编写占位符，执行前必须替换为实际测试日期，并把 `T+n 天` 换算成具体年月日后再发送，不能把占位符原样输入浏览器。每条消息单独发送并等待主回答结束和 Post-run 进入终态。正例 Radar 均须关联明确 Item / Project、使用最新完整 QA 所在 Session、最多生成一条。若主 Agent 意外指出同一缺口或提出语义相同动作，本轮记为“主回答覆盖 / 正例无效”，Radar 安静是正确行为，不记为漏触发；只有主 Agent 未覆盖时才进入正例判定。用户的窄任务只用于形成自然工作轨迹，不能显式禁止系统给出建议。

### 3.1 正例一：依赖解除后启动下一事项

**Session A**

> 用户：我们要在 T+4 天前完成星河科技官网改版。工作分为品牌素材确认、前端制作、上线验收三项。前端制作必须等 Logo、品牌色规范、产品主视觉和正式宣传文案全部验收后才能开始。现在只到了 Logo 和品牌色规范；其余素材由客户按约在 T+2 天交付，目前无需催办，也没有 Agent 可执行的准备动作。先帮我记录当前状态。

预期：形成一个 Project 及三个 Item；前端制作处于阻塞或等待状态；Radar 安静。

**Session B**

> 用户：继续星河科技官网改版。客户刚确认最终版 Logo、品牌色规范、产品主视觉和正式宣传文案全部验收无误。我要统一归档，先按“星河科技_素材类型_最终版_T”的格式列出四个文件名。

预期主回答：只列四个文件名，不提出前端任务拆分或排期。

预期 Radar：`type=next_step`、`expected_output=plan`，生成“制作前端开工任务单框架与日程草案”或等价建议，未知页面和负责人必须标记待确认，不能捏造；证据至少包含 Session A 的前端依赖与最终期限、Session B 的四项素材已经验收；目标为 Session B。

失败条件：建议再次整理文件名、重复“素材已齐”，或在 Session A 就建议开工。

### 3.2 正例二：决策条件齐备后生成比较表

**Session A**

> 用户：客户数据分析平台采购预算上限是含税 50 万元，本年度不能追加。三家候选平台的功能、数据规模、集成方式和安全合规评分已经完成，信息安全评审也已通过；现在只缺最终含税报价，报价齐备后就要完成平台选型决策。目前供应商仍在承诺期内，无需催办。先帮我记录项目背景。

预期：形成采购 Project；平台选型决策因最终报价缺失而等待；Radar 安静。

**Session B**

> 用户：继续数据分析平台采购。三家供应商的最终含税总价分别是甲 46 万元、乙 48 万元、丙 49 万元。先起草一封发给项目负责人的内部报价汇总邮件正文，准确保留三项报价。

预期主回答：完成内部报价汇总邮件；未主动生成完整选型比较表。

预期 Radar：`type=decision`、`expected_output=summary`，生成“制作平台选型决策比较表”这一明确产物，综合已有功能、集成、安全评分和最新报价并标记需确认项；证据至少包含 Session A 的决策维度已齐但报价阻塞、Session B 的三家最终报价均在预算内；目标为 Session B。笼统的“推进选型”不算命中。

失败条件：只复述报价、重复邮件，或不基于既有评估维度直接武断选择供应商。

### 3.3 正例三：交付完成后发现上线保障缺口

**Session A**

> 用户：CRM 客户门户计划在 T+6 天上线。必要事项包括开发、UAT、上线审批、回滚方案和上线后 24 小时监控安排。开发、UAT 和监控安排已完成，上线审批待业务负责人确认。按公司流程，审批通过后才能锁定生产环境和正式上线窗口，回滚方案此前无法确定；负责人仍在承诺时间内，无需催办。先帮我记录项目状态。

预期：形成 CRM 上线 Project；只剩审批和回滚方案未完成；回滚方案被审批阻塞时 Radar 安静。

**Session B**

> 用户：继续 CRM 客户门户上线。业务负责人刚刚确认审批通过，生产环境和 T+6 天上线窗口已锁定。先把这句话润色成一句适合周报的进展，控制在 35 个字以内。

预期主回答：只给一句周报进展。

预期 Radar：`type=risk`、`expected_output=checklist`，生成“制作上线回滚方案框架及待确认信息清单”这一明确产物；证据包含具体上线日期、审批刚通过、环境和窗口已锁定，以及回滚方案未完成；目标为 Session B。不得凭空生成组件、指令或负责人。

失败条件：同时展示两条建议，或只提醒“按时上线”而没有可执行产物。

### 3.4 正例四：接近完成时发现收尾确认缺口

**Session A**

> 用户：季度经营报告要在 T+3 天归档，完成标准包括数据核对、管理层意见处理、最终 PDF 和负责人签字确认。数据已核对，意见已处理，最终 PDF 也已经生成；负责人将在看到最终核对单后签字。先帮我记录当前进展。

预期：形成季度经营报告 Item，签字确认尚未完成，当前缺少最终核对单；Radar 可保持安静，因为本轮刚建立状态。

**Session B**

> 用户：继续季度经营报告。归档文件名准备使用“季度经营报告_最终版_实际日期.pdf”。先帮我检查这个命名是否清楚。

预期主回答：评价或优化文件名；未主动生成最终核对单。

预期 Radar：`type=completion`、`expected_output=checklist`，生成“制作最终验收核对单与待签字项”或等价动作；证据包含 Session A 的四项完成标准、前三项已完成且签字依赖核对单，Session B 表明正在准备归档；目标为 Session B。

失败条件：把未签字事项直接标记完成；只重复文件命名；建议重新制作已经完成的报告内容。

### 3.5 反例一：主 Agent 已覆盖同一缺口

**单 Session**

> 用户：CRM 客户门户将在 T+6 天上线，开发、UAT 和上线审批已完成，但还没有回滚方案。请审查当前上线风险，明确指出最关键的缺口，并给出可执行的处理建议。

状态前提：形成或更新明确的 CRM 上线 Item / Project，且回滚方案未完成。

主回答观察：必须明确指出缺少回滚方案，并主动提出生成或制定回滚方案；否则本 Case 不具备去重前提，记为本轮无效并重跑。

预期 Radar：安静。

失败条件：Radar 仅更换标题后再次建议生成回滚方案。标题不同但缺口、目标产物和处理动作语义相同，仍属于重复。

### 3.6 反例二：用户已经要求同一动作

**Session A**

使用正例一 Session A 建立官网改版 Project 和依赖关系。

**Session B**

> 用户：继续星河科技官网改版。四项素材已经全部验收。请制作前端开工任务单框架与日程草案，未知页面和负责人标记待确认。

预期：只有主 Agent 确实产出任务单后才进入判定；Radar 必须安静。

失败条件：Radar 再次建议制定或完善同一份前端任务单。

### 3.7 反例三：关键动作仍被阻塞

**Session A**

> 用户：官网前端制作必须等四项品牌素材全部验收后开始。目前 Logo、品牌色和主视觉已通过，正式宣传文案还在客户审核；客户仍在承诺时间内，无需催办，文案确定前也没有 Agent 可执行的准备动作。请记录进展。

**Session B**

> 用户：继续官网改版。请把已经通过的三项素材名称整理成一行，用顿号分隔。

状态前提：Event 归入明确的官网改版 Item / Project，并保留“宣传文案未通过，因此前端仍阻塞”的关系。

预期：主回答只整理三项名称；Radar 安静。

失败条件：出现任何 Radar 均失败，包括建议立即开工、催办或生成当前不可执行的准备材料。

### 3.8 反例四：一次性问答不需要持续推进

**单 Session**

> 用户：请用两句话解释 HTTPS 和 HTTP 的区别，给不懂技术的同事看。

预期：Event 进入 Non Project；Radar 安静。若错误创建 Item / Project，记 Organizer 前置失败；若已正确进入 Non Project 仍产生建议，记 Radar 误触发。

失败条件：创建持续 Item / Project，或建议制定网站安全改造计划。

### 3.9 反例五：事项已完成且没有反例

**Session A**

> 用户：年度客户满意度报告项目的问卷回收、数据分析、报告撰写和部门验收已经全部完成，所有必要事项均验收通过。请把整个项目标记为完成，只确认记录结果。

**Session B**

> 用户：继续年度客户满意度报告。请按“客户满意度报告_归档_实际日期”的格式给出一个归档文件夹名称，不要扩展其他内容。

状态前提：Project 及全部必要 Item 已完成，之后没有缺陷、延期或重开证据。

预期：Radar 安静。

失败条件：建议复盘、继续优化、补计划、再次验收或推进已经完成的事项。

### 3.10 反例六：只有信息价值而无可执行动作

**Session A**

> 用户：法务合同审核正在按计划进行，外部法务将在 T+2 天按约返回意见。目前无需催办，也没有需要提前准备的材料；收到意见后才能决定是否修改合同。请记录当前状态。

**Session B**

> 用户：请把当前状态压缩成一句不超过 30 个字的周报，不要扩展其他内容。

状态前提：事项可正确归属，但下一步完全依赖仍在承诺期内的外部结果，当前没有 Agent 可执行产物。

预期：Radar 安静。

失败条件：展示“项目进展正常”“等待法务反馈”“节点即将到来”等通知、摘要或泛泛提醒。

### 3.11 反例七：动作或执行目标无法唯一确定

测试层级：服务层策略测试，使用固定状态夹具，不依赖 Organizer 随机产生歧义。

状态夹具：同一用户有两个均处于 `in_progress` 的“客户门户上线”Item，分别属于不同 Project 和 Session；最新 Event 只写“审批通过了”，没有项目、客户或 Session 线索，无法可靠确定目标对象和执行位置。

预期：Radar 安静，不得猜测关联 Item、拼凑动作或随意选择 `target_session_id`。

失败条件：生成任何只有概率性归属的建议。

## 4. 机制守门 Case

本组的权威前提均使用固定数据库状态、Radar 记录和可控测试桩，不依赖 Analyzer 随机命中。真实浏览器流程只作为附加端到端验证。

### 4.1 新 Query 淘汰旧建议

前置步骤：固定夹具写入正例一语义等价的 `pending` Radar，动作统一为“制作前端开工任务单框架与日程草案”，并记录来源 QA 与目标 Session。

随后在同一 Session 立即发送：

> 用户：计划变更，客户暂停官网改版，恢复时间待定。请只记录这次变更。

预期：旧 Radar 变为 `superseded` 并停止展示；Organizer 正常记录暂停状态；新 QA 不应继续建议开工。

失败条件：第二轮输入后旧建议仍可见、可接受或被 Re-entry 恢复。

### 4.2 等待与 Resume 只形成一个完整 QA

测试层级：浏览器观察交互，服务层与数据库断言 Event 数、Analyzer 调用次数和 `source_run_id` 幂等；使用测试桩保证主 Agent 调用 `ask_user` 并进入等待。

前置状态：固定官网改版 Project 已完成开发与验收，但上线日期字段为空。

> 用户：请根据官网改版项目制定上线方案，并先向我确认尚未确定的上线日期。

操作与预期：

1. 主 Agent 进入 `waiting_user_input` 时，不创建 Event、不运行 Organizer，也不生成 Radar。
2. 用户补充“上线日期为 T+6 天，请继续”，Resume 最终进入 `idle` 后才执行一次 Post-run。
3. 最终只产生一条 Event，身份来自原始真实 Query，内容包含最终 Assistant 回答；`resume_answer` 或 `todolist_feedback` 不成为独立 Event。
4. Radar 只允许在最终完整 QA 后判断一次；重复 Resume 不得重复 Event 或 Radar。

失败条件：等待期间出现 Event/Radar、补充回答被记成第二个 Event，或同一 QA 重复触发 Post-run。

### 4.3 Analyzer 迟到结果不得落库

测试层级：服务层集成测试；通过可控测试桩暂停 QA1 Analyzer，浏览器手工操作不作为唯一验收方式。

1. QA1 完成并启动 Analyzer，在模型返回前暂停。
2. 同一 Session 开始 QA2，再释放 QA1 Analyzer。
3. QA1 的 Candidate 不得落库或展示；QA1、QA2 的 Organizer 仍各自正常创建 Event 和更新状态。
4. QA2 仍在 `running` 或 `waiting_user_input` 时，任何 Candidate 都不得提前落库。

失败条件：旧 Candidate 晚到后成为 `pending`、覆盖 QA2，或因新鲜度检查连带跳过 Organizer。

### 4.4 已授权动作不被新 Query 打断

测试层级：服务层集成测试；使用可控屏障分别制造 `accepted` 和 `running` 窗口。

**子 Case A：accepted 保护**

固定夹具写入 `pending` Radar，完成 `pending → accepted` 后在执行领取前暂停，再发起真实 Query。原 Radar 不得变为 `superseded`，随后只能被领取一次。

**子 Case B：running 保护**

使用固定 Radar 夹具确认状态已进入 `running`，再让新的真实 Query 成功进入正常处理链路。原动作继续执行并进入唯一终态，不被淘汰或重复领取。

失败条件：任一已授权状态被新 Query 转为 `superseded`，或同一动作执行多次。

### 4.5 Re-entry 只恢复已有建议

前置步骤：固定夹具写入有效 `pending` Radar，记录 `radar_id`、目标 Session、Event 数和状态版本。

操作：刷新页面，或离开该 Session 后重新进入。

预期：目标 Session 恢复同一个 `radar_id`；切到非目标 Session 不展示，返回目标 Session 仍恢复同一条；不新增 Event、不调用 Organizer / Analyzer、不产生第二条 Radar。再使用 4.1 让它变为 `superseded` 后 Re-entry，不得恢复旧建议。

失败条件：刷新导致重新分析、Radar 复制，或恢复已过期建议。

### 4.6 Radar 执行不递归触发

前置条件：固定夹具写入“制作前端开工任务单框架与日程草案”Radar。

操作：点击主要动作并等待 AgentV3 执行完成。

预期：原 Radar 依次进入 `accepted → running → done`；执行产物形成 Event 并更新 Item / Project；该 Radar Action Run 不再生成同类或新的 Radar。

失败条件：执行结束后自动出现“继续完善前端任务单”等递归建议，或把 Radar 的 `done` 误当成 Project 完成。

### 4.7 整理失败必须阻断 Radar

测试层级：服务层集成测试，使用固定桩让 Organizer 返回无效结构或把 Event 标记为 `classification_failed`。

预期：Event 保留在待分类；不更新 Item / Project；不调用 Analyzer，或至少不允许 Candidate 落库；前端不展示 Radar。此 Case 验证的就是失败守门，因此不记作普通“前置失败 / 本轮无效”。

失败条件：整理失败后仍保存或展示 Radar，或为了生成建议强行归属 Event。

### 4.8 同类建议与冷却期去重

测试层级：服务层策略测试，预置一个同一 Item、同一 `dedupe_key` 的未处理 Radar，或处于有效冷却期的同类历史建议。

操作：再次提交会导向同类动作的有效 Candidate。

预期：保持现有建议，不新增同类 Radar；仅修改标题不构成新建议。

失败条件：同一动作重复落库、重复计数或同时展示。

### 4.9 失效对象和过期时间过滤

测试层级：服务层查询测试，分别预置 `expires_at` 已过期、关联 Item 已完成、关联 Project 已归档的 `pending` Radar。

预期：查询时均不展示或执行旧建议，并按实现规则进入相应终态；其他仍有效的 Radar 不受影响。

失败条件：过期建议或完成/归档对象上的建议仍可见、可接受。

## 5. 记录模板与通过标准

### 5.1 覆盖矩阵

| 产品规则 | 对应 Case | 类型 / 产物 | 测试层级 |
|---|---|---|---|
| 依赖解除后发现下一步 | 3.1 | `next_step / plan` | 真实浏览器 |
| 决策条件齐备 | 3.2 | `decision / summary` | 真实浏览器；现场主 Case |
| 上线风险控制 | 3.3 | `risk / checklist` | 真实浏览器；现场备选 |
| 接近完成但仍缺确认 | 3.4 | `completion / checklist` | 真实浏览器 |
| 主 Agent 或用户已覆盖 | 3.5、3.6 | 必须安静 | 真实浏览器 |
| 阻塞、Non Project、已完成、仅信息价值 | 3.7—3.10 | 必须安静 | 真实浏览器 |
| 目标与执行 Session 不确定 | 3.11 | 必须安静 | 服务层策略测试 |
| 完整 QA、最新 QA 与过期淘汰 | 4.1—4.3 | 生命周期守门 | 服务层集成 + UI 观察 |
| 授权保护、Re-entry、防递归 | 4.4—4.6 | 生命周期守门 | 服务层集成 + UI 观察 |
| 整理失败、同类去重、对象失效 | 4.7—4.9 | 策略守门 | 服务层测试 |

### 5.2 统一运行条件

- 每轮使用全新测试账号，或清空该账号的 Project、Item、Event 和 Radar 业务数据。
- 固定代码版本、主 Agent 模型、Organizer / Radar 模型、提示词和功能开关，并记录测试基准日 `T`。
- 每条消息单独发送，确认主回答结束且 Post-run 进入成功或失败终态后再继续。
- 不复用上一轮的 Item、Project 或 Radar；Re-entry 只用于验证读取，不得用于补触发。

每次运行只记录以下字段：

| 字段 | 内容 |
|---|---|
| Case 与运行时间 | 固定 Case 编号、用户、Session、模型与代码版本 |
| 主 Agent 结果 | 未覆盖 / 已覆盖；已覆盖的正例记为“主回答覆盖 / 本轮无效” |
| 状态结果 | Event 归属、Item / Project 状态及关键证据 |
| Radar 结果 | 安静或建议标题、动作、原因、证据、目标 Session |
| 时机结果 | 是否只在完整 QA 后产生，是否正确过期或防递归 |
| 最终判定 | 通过 / 失败、主要失败原因及其他异常 |

每次先判定状态前提：不满足时记为“前置失败 / 本轮无效”，不计入 Radar 通过率，但必须单独统计前置失败率。正例再检查主 Agent 是否覆盖目标缺口；已覆盖记为“主回答覆盖 / 本轮无效”。两项前提均有效后，Radar 的出现或安静符合预期并满足第 2 节全部规则，才算单 Case 通过。

每个质量 Case 最多尝试四次，必须取得至少两次有效运行；两次有效运行都通过才算 Case 通过。四次内不足两次有效运行，记为“可测性失败”，不得从分母中删除。报告必须同时列出总尝试数、有效数、主回答覆盖数、Organizer 前置失败数和 Radar 通过数。

评测集通过要求：

- 主动判断组：四个正例至少连续两轮命中正确动作；所有有效安静反例连续两轮零误触发。
- 机制守门组：4.1—4.9 使用固定夹具分别至少连续两轮通过。
- 不允许出现重复主 Agent、阻塞期抢跑、Non Project 误触发、旧建议残留和执行递归。
- 建议措辞可以变化，但目标、动作类型、证据和目标 Session 必须与 Case 一致。
- 同时报告有效运行通过率和前置失败率，不用 Organizer 失败掩盖 Radar 失败，也不反向归责。
