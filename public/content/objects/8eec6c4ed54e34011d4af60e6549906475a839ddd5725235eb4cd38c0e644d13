# Radar 新鲜度与动作级判断实施计划

> 文档状态：代码已完成，真实模型待复测  
> 负责人：bwz  
> 最近更新：2026-07-15  
> 权威范围：Radar 旧建议失效、覆盖判断、约束判断和专项验收

[返回文档总览](../README.md)

## 1. 目标与原则

本专项解决三个问题：

1. 其他 Session 更新了 Item / Project 后，依赖旧状态的 Radar 不能继续显示或被接受。
2. 代用户生成的正文、引用、模板或泛化邀约，不能被误判为主 Agent 已直接覆盖同一推进动作。
3. 用户对某个动作的限制不能被扩大到不同的准备、比较或核对动作。

统一原则：不添加采购、邮件、报价等场景关键词规则；所有行为由状态版本和动作级结构字段决定。

## 2. 新鲜度与状态版本

- [x] 新真实 Query 开始时，当前 Session 的 `pending/snoozed` Radar 进入 `superseded`。
- [x] Analyzer 调用前和返回后校验来源 Event 仍对应该 Session 最新真实 QA。
- [x] `radar_items` 保存 `source_item_version` 和可选的 `source_project_version`。
- [x] Organizer、普通 Item / Project 更新和人工纠错提交新版本时，在同一事务内淘汰依赖旧版本的 `pending/snoozed` Radar，覆盖跨 Session 情况。
- [x] Radar 落库事务复核版本；分析期间状态变化时返回 `stale`，Candidate 不落库。
- [x] 读取和接受接口再次复核版本，防止人工纠正或迟到写入留下旧建议。
- [x] `accepted/running` 已获用户授权，不因新 Query 或状态更新自动中断。

迁移规则：历史未接受 Radar 没有可靠来源版本，读取时按旧状态处理并进入 `superseded`；历史已授权动作不被追溯中断。

## 3. 动作级 Analyzer 协议

生产 Analyzer 返回：

```json
{
  "opportunity": {
    "action_key": "稳定动作键",
    "action": "具体动作",
    "expected_output": "draft | checklist | plan | summary"
  },
  "coverage": {
    "kind": "none | delivered | explicit_recommendation | user_requested | generic_offer | artifact_text",
    "source": "none | assistant_direct | user_request | generated_artifact",
    "evidence": "短证据或 null"
  },
  "constraint_check": {
    "status": "allowed | blocked",
    "reason": "约束是否命中当前动作"
  },
  "feasibility": "ready | insufficient_evidence | not_actionable",
  "candidate": "allowed 且 ready 时为完整 Candidate，否则为 null"
}
```

服务端而非模型负责最终映射：

| 条件 | 决策 |
|---|---|
| `ready + allowed`，且覆盖为 `none/generic_offer/artifact_text` | 生成 `actionable_gap` |
| `ready + allowed`，且覆盖为 `delivered/explicit_recommendation/user_requested` | 静默 `main_answer_covered` |
| `insufficient_evidence` | 静默 `insufficient_evidence` |
| 无机会、被约束阻止或不可推进 | 静默 `not_actionable` |

Analyzer 对所有 `ready + allowed` 的机会都返回 Candidate，即使它认为动作已经覆盖；Policy Gate 在确认覆盖时丢弃 Candidate。这样覆盖分类与最终推送决策分离。

## 4. 覆盖与约束规则

- `delivered`：主 Agent 已直接完成同一动作。
- `explicit_recommendation`：主 Agent 在生成产物之外直接建议同一动作。
- `user_requested`：用户已经明确要求同一动作。
- `artifact_text`：动作只出现在代用户生成、引用或模拟的内容内部，不算覆盖。
- `generic_offer`：只是条件式或泛化邀约，不算覆盖。
- `constraint_check` 只比较约束与当前 `opportunity`；不得把“不要代我做最终决定”扩大成“不要准备比较材料”。它与 `feasibility` 独立：动作即使为 `ready`，命中明确禁止时仍静默。

## 5. 实现与验证状态

主要实现：

- `backend/database.py`：版本字段、事务内失效、落库竞态守门和结构化审计字段。
- `backend/radar_service.py`：协议校验、确定性 Policy Gate、版本校验、动作语义去重和最新 QA 分析状态查询。
- `backend/radar_post_run.py`：生产 Analyzer Prompt 与结构化输出约束。
- `frontend/src/components/radarDisplayState.js`：依据最新 QA 的分析状态执行最长约 63 秒的有界轮询；建议出现或静默决策落定后停止，Re-entry 只查询一次。

自动化结果：Radar 后端 227 项、前端 Radar 27 项通过，Ruff 通过，生产构建成功。覆盖场景包括跨 Session 状态更新、Analyzer 期间竞态、读取与接受防线、`artifact_text`、`generic_offer`、直接覆盖、动作级允许/阻止、已授权动作保护，以及“继续等待分析 / 静默落定立即停止”的前端时序。扩展集成套件为 273 项通过、6 个既有 Context Pipeline 用例失败；失败集中在日期记忆模板、日期顺序和 Todolist 修订语义，与本次 Radar 修改无关。

## 6. 真实复测

已使用全新账号执行修订后的《主动判断评测集》3.2：

1. 旧版 Session A 没有实际评分，审计为 `insufficient_evidence`，保持安静正确；修订输入必须提供三家四维评分。
2. 修订版 Session A 正确建立采购 Project 与唯一当前 Item“平台选型”；Session B 补齐报价，并只要求主 Agent 起草报价汇总正文。
3. Session B 审计为 `coverage.kind=generic_offer`、`constraint_check=allowed`、`feasibility=ready`、`outcome=created`，成功生成比较材料类 Radar，来源 Item / Project 版本均为 2。
4. 旧前端因 Post-run 约 35 秒而轮询提前结束，切换 Session 后通过 Re-entry 才显示；现已完成状态感知轮询实现和自动化验证。
5. 2026-07-15 使用全新账号完成最终复测：Session B 主回答结束后保持在当前页面，约 20 秒后自动显示“整理评分+价格一体化对照表”；数据库记录 `outcome=created`、`reason_code=actionable_gap`，来源 Item / Project 版本均为 2。状态感知轮询完整通过。
