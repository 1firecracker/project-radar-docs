# 消息列表规则与状态边界

## 1. 先说结论

后续底层不应再把 OpenAI 风格的 `messages` 当作唯一真相，也不应再用一个笼统的 `conversation_state` 装下所有内容。

应该拆成 5 层：

1. `message_records`
   - 消息与事件真相
2. `asset_index`
   - 文件与解析产物索引
3. `runtime_control`
   - 当前运行控制信息
4. `session_metadata`
   - 会话元数据
5. `projections`
   - 为编排和恢复准备的投影视图

发送给 LLM 的 `messages` 只是编排结果，不是底层真相。

## 2. 数据分层

### 2.1 `message_records`

这是唯一的消息真相层，负责记录“发生过什么”。

它应该是追加式的事件流，至少覆盖：

- 原始用户输入
- 用户上传文件的发生时刻
- 当前消息触发的 `knowledge_base_refs`
- `ask_user` 的发起与回答
- 用户手动编辑 `todolist`
- assistant 回复
- tool call / tool result
- summary
- 主线程可见的 subtask 挂载点或结果摘要

这里的重点是：  
“这件事发生过”，就应该在 `message_records` 里留下记录。

### 2.2 `asset_index`

这是文件索引层，不是消息层，也不是运行控制层。

它负责登记：

- 用户上传的文件
- 工具生成的新文件
- 文件的稳定 `file_id`
- 文件在宿主机与沙盒里的路径
- 解析后的派生产物
- 解析产物与源文件的关联关系

这里的重点是：  
文件是资产，不是消息文本，也不是运行时状态。

### 2.3 `runtime_control`

这是运行控制层，只放“当前系统正在怎样跑”。

它负责：

- 当前是否 `running`
- 当前是否 `waiting_user_input`
- 当前是否 `paused`
- 当前是否 `terminating`
- 当前是否 `terminated`
- 当前这一轮识别出的输入语言
- 当前这一轮实际选中的 prompt locale
- 当前这一轮实际选中的 tool locale
- 当前挂起的是哪一张 `ask_user` 卡片
- 当前暂停或终止的原因
- 当前 run 的错误信息
- 当前有哪些子任务在排队、运行、完成或失败

这里的重点是：  
这些信息服务于编排器、后端 worker 和前端控制，不是 prompt 真相。

### 2.4 `session_metadata`

这是纯元数据层，只放不会直接参与推理的会话信息。

例如：

- `conversation_title`
- `preferred_locale`
- `created_at`
- `updated_at`
- `owner_user_id`
- 可能的语言偏好或展示偏好

这里的重点是：  
元数据是元数据，不该被说成状态真相。

### 2.5 `projections`

这是投影视图层，用来加速恢复和 prompt 编排。

它可以缓存这些可重建结果：

- `current_todolist_projection`
- `available_files_projection`
- `current_active_editor_projection`
- `current_language_profile`
- `latest_summary_projection`

这里的重点是：

- `projections` 可以持久化成缓存
- 但它不是唯一真相
- 大部分投影可以从 `message_records + asset_index` 重新计算出来
- 当前活动编辑器属于会话当前态，应由前端/运行时直接更新到投影层

## 3. `message_records` 规则

### 3.1 基础结构

每条消息记录至少包含：

```json
{
  "id": "msg_001",
  "created_at": "2026-03-16T10:00:00+08:00",
  "type": "user_input",
  "language": "zh-CN",
  "text": "请分析我上传的报表",
  "payload": {},
  "uploads": [],
  "refs": [],
  "subtask_id": null,
  "parent_id": null
}
```

建议补充字段：

- `input_kind`
- `language`
- `language_confidence`
- `tool_name`
- `tool_call_id`
- `summary_kind`
- `source`
- `version`

### 3.2 `type` 的主分类

先收敛成少量稳定类型：

- `system_context`
- `user_input`
- `assistant_output`
- `tool_call`
- `tool_result`
- `summary`
- `subtask_summary`
- `audit_event`

说明：

- `audit_event` 默认不直接发给 LLM，只用于审计和排障
- `subtask_summary` 只负责给主线程或前端一个挂载点
- 子任务内部 assistant/tool 循环不直接平铺进主 `message_records`

### 3.3 `user_input` 的子类型

`user_input` 是一个族，不是只有普通文本一种。

当前至少预留这些子类型：

- `text`
  - 普通用户文本
- `resume_answer`
  - `ask_user` 的补充回答
- `todolist_edit`
  - 用户手动编辑 `todolist`
- `upload_only`
  - 用户只上传文件，没有额外自然语言

说明：

- 当前活动编辑器不再作为 `user_input` 子类型
- 它属于会话级当前态，应进入 `projections.current_active_editor_projection`
- 如果后续需要保留“编辑器切换历史”，更适合用 `audit_event`，而不是主消息

### 3.4 `todolist_edit` 的接口

你刚才强调的这一点要单独钉死：

- 它的顶层 `type` 和普通用户消息一样，都是 `user_input`
- 编排给 LLM 时也一样是 `role=user`
- 区别只在于 `input_kind=todolist_edit`
- 真正的 todolist 修改内容进入 `payload`

建议结构：

```json
{
  "id": "msg_user_todolist_001",
  "created_at": "2026-03-16T10:05:00+08:00",
  "type": "user_input",
  "input_kind": "todolist_edit",
  "text": "用户修改了当前 todolist",
  "payload": {
    "operation": "replace",
    "source": "manual_editor",
    "todo_list": [
      {
        "content": "先核对消息模型",
        "status": "in_progress"
      },
      {
        "content": "补充终止态",
        "status": "todo"
      }
    ]
  },
  "uploads": [],
  "refs": [],
  "subtask_id": null,
  "parent_id": null
}
```

这条消息的语义是：

- 它是一条真实用户操作
- 它会驱动 `current_todolist_projection`
- 但它自己仍然是消息，不是“全局状态块”

### 3.5 `ask_user` 的记录方式

`ask_user` 不应该再被说成“整个都是会话级状态”。

正确拆法是：

1. assistant 发起 `ask_user`
   - 记录为一条 `tool_call`
2. `ask_user` 卡片内容落库
   - 记录为一条 `tool_result`
3. `runtime_control.pending_ask_user_message_id`
   - 只保存“当前挂起的是哪一条 ask_user 消息”
4. 用户补充回答
   - 回填到对应的 `ask_user` `tool_result`
   - 在该 `tool_result.payload.result.answers` 中保存答案

也就是说：

- `ask_user request/answer` 是消息真相
- “当前还有没有 ask_user 没完成”是运行控制

## 4. 文件与引用规则

### 4.1 用户上传文件

用户上传文件属于消息级事实，应该挂在上传发生的那一条 `user_input` 上。

建议结构：

```json
{
  "uploads": [
    {
      "file_id": "file_001",
      "name": "report.xlsx",
      "asset_kind": "user_upload",
      "storage_path": "/workspace/session_files/report.xlsx",
      "sandbox_path": "/mnt/data/report.xlsx",
      "mime_type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
      "upload_turn": 1,
      "stable_order": 1
    }
  ]
}
```

要求：

- 必须有稳定 `file_id`
- 必须有稳定顺序字段
- 不再靠“旧注入消息残留”保持文件上下文
- 文件连续性由 `message_records + asset_index + available_files_projection` 保证

### 4.2 `asset_index` 的职责

`asset_index` 才是文件连续性的权威索引。

建议结构：

```json
{
  "files": [
    {
      "file_id": "file_001",
      "origin_message_id": "msg_001",
      "asset_kind": "user_upload",
      "name": "report.xlsx",
      "storage_path": "/workspace/session_files/report.xlsx",
      "sandbox_path": "/mnt/data/report.xlsx",
      "stable_order": 1
    }
  ],
  "parsed_artifacts": [
    {
      "artifact_id": "parsed_001",
      "source_file_id": "file_001",
      "parser": "document_parser",
      "storage_path": "/workspace/session_files/parser/result.md"
    }
  ]
}
```

这里替代了之前那种泛化的：

- `saved_files_state`
- `parsed_files_state`
- `context_archives_state`（专门记录 summary 上下文归档，不和普通 offload 文件混用）

其中前两项已被更明确的文件索引取代；`context_archives_state` 则是单独的 summary 归档索引，不属于普通文件索引。

### 4.3 `knowledge_base_refs`

`knowledge_base_refs` 是消息级 sidecar，不是文件索引，也不是运行控制。

建议结构：

```json
{
  "refs": [
    {
      "ref_type": "knowledge_base",
      "path": "/project/docs",
      "match_text": "@知识库/docs"
    }
  ]
}
```

规则：

- 只挂在对应的 `message_record`
- 不进入 `runtime_control`
- 不进入 `session_metadata`
- 不进入全局文件索引

## 5. `runtime_control` 规则

这一层只放运行控制，不放消息真相，不放文件清单，不放完整 `todolist` 内容。

当前已实现的结构（见 `empty_conversation_state()`）：

```json
{
  "conversation_status": "idle",
  "current_prompt_locale": null,
  "supports_ask_user": true,
  "pending_ask_user_message_id": null,
  "pending_ask_user_request_id": null,
  "pending_subtask_ids": [],
  "active_subtask_id": null,
  "subtask_counter": 0,
  "reflection_counter": 0,
  "subtasks": {},
  "pause_reason": null,
  "termination_requested_at": null,
  "termination_reason": null,
  "last_error": null,
  "active_agent": "main_agent",
  "global_loops": 0,
  "global_loops_limit": null
}
```

### 5.1 这里应该放什么

当前已实现的字段（见 `empty_conversation_state()`）：

- `conversation_status`
- `current_prompt_locale`
- `supports_ask_user`
- `pending_ask_user_message_id`
- `pending_ask_user_request_id`
- `pending_subtask_ids`
- `active_subtask_id`
- `subtask_counter`
- `reflection_counter`
- `subtasks`
- `active_agent`
- `global_loops`
- `global_loops_limit`
- `pause_reason`
- `termination_requested_at`
- `termination_reason`
- `last_error`

已被标记为冗余并自动移除的字段：

- `active_run_id`
- `current_input_locale`
- `current_tool_locale`

### 5.2 这里不应该放什么

不应该放：

- 用户上传文件列表
- `knowledge_base_refs`
- `pending_ask_user_answer` 的完整正文
- 完整 `todolist` 数据
- 完整 `active_editor` 内容
- 解析文件列表

这些要么属于消息真相，要么属于文件索引，要么属于投影缓存。

### 5.3 `runtime_control.subtasks`

子任务不能只当消息看，它还需要调度状态和自己的内部循环。

因此应当在 `runtime_control` 里单独维护：

```json
{
  "pending_subtask_ids": ["subtask_002"],
  "active_subtask_id": "subtask_001",
  "subtask_counter": 2,
  "subtasks": {
    "subtask_001": {
      "subtask_id": "subtask_001",
      "status": "running",
      "title": "分析附件并整理结论",
      "parent_tool_call_id": "call_123",
      "instruction": "只分析附件内容，不改主计划",
      "loop_count": 2,
      "max_loops": 5,
      "messages": [
        {
          "role": "assistant",
          "content": "先读取附件内容并列出关键字段。"
        },
        {
          "role": "tool",
          "name": "document_parser",
          "content": "{...}"
        }
      ],
      "result": null,
      "error": null
    }
  }
}
```

这层的关键约束是：

- 用户不能直接和子任务互动
- 子任务内部没有普通 `user_input`
- 子任务消息循环只需要 `assistant` 和 `tool`
- 子任务指令可以单独放在 `instruction` 字段，不必伪装成用户消息
- 子任务完成后，再把结果摘要回填到主线程可见位置

### 5.4 状态枚举

当前至少预留：

- `running`
- `waiting_user_input`
- `paused`
- `terminating`
- `terminated`
- `idle`
- `error`

其中：

- `paused` 用于主动暂停或工具级等待
- `terminating` 用于用户点击中止后、worker 还没收尾完成
- `terminated` 用于明确结束且不可继续
- 子任务自身还应额外支持 `queued | running | succeeded | failed | rejected | cancelled`

## 6. `session_metadata` 规则

这一层只放元数据，不放编排内容。

建议结构：

```json
{
  "conversation_title": null,
  "preferred_locale": null,
  "created_at": "2026-03-16T10:00:00+08:00",
  "updated_at": "2026-03-16T10:05:00+08:00"
}
```

说明：

- `conversation_title` 属于元数据，不属于运行控制
- 它不该被误写成“会话状态真相”
- `preferred_locale` 只是长期偏好，不是当前轮的最终语言决策

## 7. `projections` 规则

这层是投影视图，不是唯一真相。

当前已实现的结构（由 `_SUPPORTED_PROJECTION_FIELDS` 白名单控制）：

```json
{
  "current_active_editor_projection": null
}
```

以下字段在规划中但尚未作为 projection 实现（`ensure_conversation_state()` 会过滤掉不在白名单内的字段）：

- `current_todolist_projection`：todolist 已独立为顶层字段
- `available_files_projection`
- `current_language_profile`
- `latest_summary_message_id`

原规划建议结构（供后续参考）：

```json
{
  "current_todolist_projection": [],
  "available_files_projection": [],
  "current_active_editor_projection": null,
  "current_language_profile": null,
  "latest_summary_message_id": null
}
```

### 7.1 `current_todolist_projection`

它是从这些记录推导出来的当前视图：

- `todolist_edit`
- todolist 工具更新消息
- 可能的 subtask 结果回填

它可以持久化缓存，但必须可重建。

### 7.2 `available_files_projection`

它是当前对话仍然可引用的文件集合视图。

它由这些来源推导：

- 用户上传文件
- 工具生成文件
- 文件删除或失效事件
- `asset_index`

它解决的是“summary 之后文件上下文怎么保持”的问题，但它自己不是文件真相。

### 7.3 `current_active_editor_projection`

当前活动编辑器属于会话当前态，不应和某一条用户消息强绑定。

建议结构：

```json
{
  "path": "/workspace/app.py",
  "language": "python",
  "dirty": true,
  "version": 12,
  "selection": {
    "start": 120,
    "end": 180
  }
}
```

规则：

- 它进入 `projections`
- 它由前端或运行时直接更新
- 它不是普通 `user_input`
- 如果需要记录“谁在什么时候切换了编辑器”，再额外补 `audit_event`

### 7.4 `current_language_profile`

当前轮语言不再由 `conf.runtime.language` 直接定死，而是由最近一次有效用户输入驱动。

建议结构：

```json
{
  "detected_locale": "zh-CN",
  "source_message_id": "msg_001",
  "confidence": 0.98,
  "prompt_locale": "zh-CN",
  "tool_locale": "zh-CN",
  "fallback_chain": ["zh-CN", "zh", "default"]
}
```

说明：

- `detected_locale` 是从最新用户输入检测出的语言
- `prompt_locale` 是本轮实际选中的 prompt bundle
- `tool_locale` 是本轮实际选中的工具 schema / tool 文案 locale
- `fallback_chain` 用于缺少精确匹配时的降级
- 这是一份投影，不是唯一真相；原始语言事实仍然记录在 `message_records.language`

## 8. 发给 LLM 时的编排规则

LLM `messages` 必须由唯一编排器从上面几层统一生成，不能这里拼一点、那里拼一点。

编排输入：

- `message_records`
- `asset_index`
- `runtime_control`
- `session_metadata`
- `projections`

### 8.1 类型到 role 的映射

- `system_context` -> `system`
- `user_input` -> `user`
- `assistant_output` -> `assistant`
- `tool_call` -> `assistant`
- `tool_result` -> `tool`
- `summary` -> `assistant`
- `subtask_summary` -> 主线程中的摘要卡或挂载点

### 8.2 编排顺序

1. 固定 `system_context`
2. 当前保留的 `summary`
3. 预算内的 `message_records`
4. 对每条 `user_input` 附带其 `uploads` / `refs`
5. 在固定位置注入必要的 `projections`
6. 子任务内部消息默认不直接进入主线程 prompt
7. 主线程吸收子任务结果时，优先依赖已回填的 `tool_result`

### 8.3 哪些东西不直接进 prompt

默认不直接进 prompt：

- `runtime_control`
- `session_metadata`
- `asset_index` 原始全量结构
- 子任务内部的原始 `messages`

它们只能通过编排器，转成当前轮真正需要的上下文块。

换句话说：

- `runtime_control` 用于控制系统，不用于充当模型真相
- `asset_index` 用于查文件，不用于整包塞给模型
- `session_metadata` 用于展示和管理，不用于直接推理
- 子任务原始循环主要服务于调度和前端展开，不默认进入主线程上下文

### 8.4 按用户输入动态选择 prompt 和工具语言

这个需求要明确成硬规则：

- 当前轮 prompt 语言由最新有效 `user_input.language` 决定
- 当前轮 tool schema / tool 文案语言也由同一轮语言路由决定
- 不再由 `conf.runtime.language` 直接限制只能走中文或英文
- `conf` 最多只提供默认值、fallback 和可用 bundle 注册，不负责决定当前轮语言

建议路由步骤：

1. 读取最新一条有效 `user_input`
2. 检测其 `language`
3. 生成 locale fallback chain
   - 例如 `zh-CN -> zh -> default`
   - 例如 `en-US -> en -> default`
4. 从 prompt bundle registry 中选择本轮 prompt
5. 从 tool family registry 中选择本轮 tool schema locale
6. 将本轮决策写入：
   - `runtime_control.current_input_locale`
   - `runtime_control.current_prompt_locale`
   - `runtime_control.current_tool_locale`
   - `projections.current_language_profile`

这里有两个额外要求：

- “完全不同的 prompt”是允许的
  - 不要求不同语言之间只是翻译关系
  - 每个 locale 可以有完全独立的 prompt 结构和规则
- 工具语言也应当按 locale 动态切换
  - 不再依赖 `*_zh` / `*_en` 写死在 conf 里
  - 更合理的做法是“tool family + locale variant”运行时解析

建议把 prompt 和工具都抽象成 registry：

```json
{
  "prompt_bundles": {
    "zh-CN": "agents/agentv3/system_prompts/zh-CN/main.md",
    "en-US": "agents/agentv3/system_prompts/en-US/main.md",
    "default": "agents/agentv3/system_prompts/default/main.md"
  },
  "tool_locale_policy": {
    "web_search": {
      "zh-CN": "zh-CN",
      "en-US": "en-US",
      "default": "default"
    }
  }
}
```

也就是说：

- conf 不再决定“这次一定中文还是英文”
- conf 只负责声明有哪些 bundle 和哪些工具家族可用
- 当前轮到底走哪种语言，由输入路由决定

## 9. `summary` 的规则

`summary` 的职责是压缩语义，不是接管全部真相。

它应该记录：

- 当前总体目标
- 关键知识
- 最近完成了什么
- 哪些文件在语义上很重要
- 当前 `todolist` 的语义进展
- 已完成子任务的重要结果

但它不应该成为这些内容的唯一权威来源：

- 文件权威来源仍然是 `asset_index`
- `todolist` 当前视图仍然是 `current_todolist_projection`
- `ask_user` 是否挂起仍然看 `runtime_control`
- 子任务是否仍在排队/运行仍然看 `runtime_control.subtasks`
- 当前轮到底使用哪种 prompt / tool locale 仍然看 `runtime_control` 与 `current_language_profile`

### 9.1 一个可接受的 `summary` 结构

```xml
<summary>
  <overall_goal>...</overall_goal>
  <key_knowledge>...</key_knowledge>
  <recent_actions>...</recent_actions>
  <important_files_notes>...</important_files_notes>
  <todolist_progress_notes>...</todolist_progress_notes>
</summary>
```

注意：

- 它可以提“用户上传过哪些重要文件”
- 也可以提“当前 todolist 推进到了哪里”
- 但这仍然是语义摘要，不是底层权威索引

## 10. 子任务与前端折叠规则

`subtask` 不应在主时间线上完全平铺，主时间线也不应该直接存一整套子任务对话。

正确展示方式：

- 主时间线只显示一张 `subtask` 摘要卡
- 摘要卡默认折叠
- 展开后再读取 `runtime_control.subtasks[subtask_id].messages`
- 多个 `subtask` 以 `subtask_id` 分组，不和主线消息互相穿插

因此：

- 主线程 `message_records` 只需要保留 `subtask_summary`
- 子任务详细消息放在 `runtime_control.subtasks`
- `subtask_summary` 至少要有 `subtask_id`、`parent_id`、`subtask_title`、`subtask_status`

## 11. 当前还容易漏掉的内容

除了 `todolist_edit`，当前还要一起考虑这些边界：

- `ask_user` 的请求消息和回答消息
- 用户只上传文件、不输入文字的场景
- 当前轮活动编辑器快照
- 工具生成文件和用户上传文件的区分
- `stop` 发出后但 worker 尚未结束的过渡态
- `paused` 与 `terminated` 的明确区分
- 子任务队列、活跃子任务、子任务失败或拒绝状态
- `summary` 之后文件和 todolist 如何继续稳定进入 prompt

## 12. 已知问题

### 12.1 前端没有明确暂停态显示

当前前端虽然维护了 `conversationStatus`，但没有清晰展示：

- 正在等待补充信息
- 已暂停
- 正在终止
- 已终止

用户能感知到的状态信息还不够。

### 12.2 点击中止后没有完整的 `terminating -> terminated` 闭环

当前中止流程更像是：

- 给 worker 发 `stop_event`
- 等待 worker 自己退出

但缺少独立的终止态协议，所以用户视角会混成：

- 像暂停
- 或回到 `idle`
- 或保留上一次的等待态

### 12.3 `todolist` 手动编辑还不是一等消息

当前实现里，手动修改 `todolist` 仍主要通过注入 patch 传递，还没有正式落成 `user_input / input_kind=todolist_edit` 的消息记录。

### 12.4 文件连续性仍未完全切到 `file_id`

当前文件合并仍有按 `path` 处理的旧逻辑，尚未完全切到稳定 `file_id + stable_order` 驱动。

### 12.5 当前语言选择仍强绑定 `conf`

当前代码仍然主要依赖这些耦合点：

- `runtime.language`
- `runtime.system_prompt_zh / system_prompt_en`
- tool loader 里的 `*_zh / *_en` 变体选择

这意味着：

- 语言主要在启动或加载时决定
- 还不是基于每轮用户输入动态路由
- 还隐含只支持中英文的分支假设

## 13. 推荐落地顺序

建议按下面顺序实现，别一次改到爆：

1. 先把底层拆成 `message_records + asset_index + runtime_control + session_metadata + projections`
2. 把 `todolist_edit` 做成正式 `user_input`
3. 把用户上传文件正式挂到消息记录，并引入稳定 `file_id`
4. 把 `knowledge_base_refs` 彻底固定成消息级 sidecar
5. 把 `ask_user` 改成“消息记录 + runtime pointer”结构
6. 把语言选择改成“按 user_input 动态路由 prompt bundle + tool locale”
7. 给前后端补齐 `paused / terminating / terminated`
8. 把子任务内部消息迁入 `runtime_control.subtasks`
9. 最后再改前端 `subtask` 折叠展示

## 14. 与旧文档冲突时以什么为准

如果后续实现与旧写法冲突，以本文件的这几个边界为准：

- 消息真相放在 `message_records`
- 文件真相放在 `asset_index`
- 运行控制放在 `runtime_control`
- 标题等元数据放在 `session_metadata`
- `todolist`、可用文件、活动编辑器优先视为 `projections`
- 子任务详细消息和运行状态优先放在 `runtime_control.subtasks`
- 当前轮 prompt / tool 语言由输入路由决定，不由 conf 直接定死
- LLM `messages` 永远只是编排结果
