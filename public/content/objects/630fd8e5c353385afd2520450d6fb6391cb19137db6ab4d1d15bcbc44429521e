# Radar 主动判断评测集

> 文档状态：评审中  
> 负责人：bwz  
> 最近更新：2026-07-14  
> 权威范围：Radar 应该提醒与必须安静的固定评测 Case、判定口径和通过标准

[返回文档总览](./README.md)

## 1. 评测目标

本评测集只回答一个问题：**完整 QA 结束并完成状态整理后，Radar 是否发现了用户尚未提出、主 Agent 尚未覆盖、现在又确实值得执行的一步。**

它不把 Organizer 的基础归属准确率计入 Radar 得分，也不把摘要、记忆召回或普通下一步建议算作 Radar 价值。每次运行先验证状态前提：前提不成立记为“前置失败 / 本轮无效”，单独记录 Organizer 问题；只有状态前提成立后，才评价 Radar。

## 2. 统一判定口径

一条 Radar 只有同时满足以下条件才算正确：

1. **新价值**：不是用户本轮明确要求的动作，也不是主 Agent 最终回答已经指出的同一缺口。
2. **当前可推进**：前置条件已满足，动作现在可以由 Agent 执行；仍被阻塞时必须安静。
3. **状态相关**：关联明确的 Item 或 Project，不从 Non Project 或待分类内容生成。
4. **可执行**：动作能产生清单、计划、草稿或总结，不能只有通知、摘要或泛泛提醒。
5. **可解释**：至少一条证据可追溯到 Event，并指向明确的目标 Session。
6. **时机正确**：只在最新完整 QA 的 Post-run 后产生；流式回答、等待用户和 Re-entry 时不新生成。
7. **不过期**：新 Query 淘汰旧的 `pending/snoozed` 建议，但不打断已授权的 `accepted/running` 动作。
8. **不递归**：Radar Action Run 更新状态并产生 Event，但不再生成 Radar。

判定优先级：**先判断是否必须安静，再评价建议内容。** 不该出现却出现的建议，标题再好也算失败。

## 3. 主动判断质量 Case

统一规则：测试开始日记为 `T`；每条消息单独发送并等待主回答结束和 Post-run 进入终态。正例 Radar 均须关联明确 Item / Project、使用最新完整 QA 所在 Session、最多生成一条。每个 Case 按“状态前提 → 主回答观察 → Radar 预期 → 失败条件”判定。

### 3.1 正例一：依赖解除后启动下一事项

**Session A**

> 用户：我们要在 T+4 天前完成星河科技官网改版。工作分为品牌素材确认、前端制作、上线验收三项。前端制作必须等 Logo、品牌色规范、产品主视觉和正式宣传文案全部验收后才能开始。现在只到了 Logo 和品牌色规范；其余素材由客户按约在 T+2 天交付，目前无需催办，也没有 Agent 可执行的准备动作。请只记录当前状态，不要制定后续计划。

预期：形成一个 Project 及三个 Item；前端制作处于阻塞或等待状态；Radar 安静。

**Session B**

> 用户：继续星河科技官网改版。客户刚确认最终版 Logo、品牌色规范、产品主视觉和正式宣传文案全部验收无误。我要统一归档，请只按“星河科技_素材类型_最终版_T”的格式列出四个文件名，不要扩展其他内容。

预期主回答：只列四个文件名，不提出前端任务拆分或排期。

预期 Radar：生成一条“制定前端开工任务单并排期”或等价建议；证据包含四项依赖刚满足和 T+4 天期限。

失败条件：建议再次整理文件名、重复“素材已齐”，或在 Session A 就建议开工。

### 3.2 正例二：采购条件满足后发现安全前置项

**Session A**

> 用户：客户数据分析平台采购预算上限是含税 50 万元，本年度不能追加。采购分为预算确认、信息安全评审、平台选型和正式申请。预算已经确认，但信息安全评审必须附最终供应商报价，报价尚未确定，所以评审暂时无法启动；评审通过前不能正式采购。目前供应商仍在承诺期内，无需催办。请只把这些作为项目背景记录下来。

预期：形成采购 Project；信息安全评审为未完成的必要 Item；Radar 安静。

**Session B**

> 用户：继续数据分析平台采购。供应商最终含税报价是服务器 15 万、软件许可 20 万、实施服务 11 万。请只起草一封提交采购部门的申请邮件正文，保留三项报价，不提供邮件以外的建议。

预期主回答：只写采购申请邮件；不主动补充安全评审动作。

预期 Radar：生成“准备信息安全评审材料清单”或等价动作；证据包含最终报价已解除材料阻塞、安全评审是正式采购前置条件，以及报价合计 46 万元未超预算。

失败条件：只建议“制定选型清单”，忽略明确前置项；或者重复主回答中的报价和邮件。

### 3.3 正例三：交付完成后发现上线保障缺口

**Session A**

> 用户：CRM 客户门户计划在 T+6 天上线。必要事项包括开发、UAT、上线审批、回滚方案和上线后 24 小时监控安排。开发和 UAT 已完成，上线审批待业务负责人确认。按公司流程，审批通过后才能锁定生产环境和正式上线窗口，回滚方案与监控排班此前无法确定；负责人仍在承诺时间内，无需催办。请只记录项目状态。

预期：形成 CRM 上线 Project；审批、回滚方案、监控安排均未完成；被审批阻塞时 Radar 安静。

**Session B**

> 用户：继续 CRM 客户门户上线。业务负责人刚刚确认审批通过，生产环境和 T+6 天上线窗口已锁定。请把这句话润色成一句适合周报的进展，控制在 35 个字以内，不要给其他建议。

预期主回答：只给一句周报进展。

预期 Radar：从回滚方案和监控安排中选择最高价值的一项，生成明确产物，例如“生成上线回滚方案”；证据包含 T+6 天期限、审批刚通过、环境和窗口已锁定，以及该必要事项未完成。

失败条件：同时展示两条建议，或只提醒“按时上线”而没有可执行产物。

### 3.4 正例四：完成事项因反例重新打开

**Session A**

> 用户：移动端客户反馈表单已经完成开发、测试和上线验收，可以把“移动端反馈表单上线”事项标记为完成。请只确认记录结果。

预期：Item 完成；Radar 安静。

**Session B**

> 用户：继续移动端反馈表单。今天发现 iPhone SE 上提交按钮被底部导航遮挡，用户无法提交。请只把这条问题改写成一句缺陷标题，不要给修复方案。

预期主回答：只输出缺陷标题；Item 因明确反例重新打开。

预期 Radar：生成“制定移动端修复与回归检查清单”或等价动作；证据包含已上线事项出现阻断提交的反例。

失败条件：仍把 Item 保持完成，或建议与缺陷无关的产品规划。

### 3.5 反例一：主 Agent 已覆盖同一缺口

**单 Session**

> 用户：CRM 客户门户将在 T+6 天上线，开发、UAT 和上线审批已完成，但还没有回滚方案。请审查当前上线风险，明确指出最关键的缺口，并给出可执行的处理建议。

状态前提：形成或更新明确的 CRM 上线 Item / Project，且回滚方案未完成。

主回答观察：必须明确指出缺少回滚方案，并主动提出生成或制定回滚方案；否则本 Case 不具备去重前提，记为本轮无效并重跑。

预期 Radar：安静。

失败条件：Radar 仅更换标题后再次建议生成回滚方案。

### 3.6 反例二：用户已经要求同一动作

**单 Session**

> 用户：官网素材已经全部齐备。请直接制定前端开工任务单，包含页面拆分、负责人和 7 月 18 日前的排期。

预期：主 Agent 完成任务单后，Radar 安静。

失败条件：Radar 再次建议制定或完善同一份前端任务单。

### 3.7 反例三：关键动作仍被阻塞

**Session A**

> 用户：官网前端制作必须等四项品牌素材全部验收后开始。目前 Logo、品牌色和主视觉已通过，正式宣传文案还在客户审核；客户仍在承诺时间内，无需催办，文案确定前也没有 Agent 可执行的准备动作。请记录进展。

**Session B**

> 用户：继续官网改版。请把已经通过的三项素材名称整理成一行，用顿号分隔。

预期：主回答只整理三项名称；Radar 安静。

失败条件：Radar 建议立即制定前端开工任务单或启动前端制作。

### 3.8 反例四：一次性问答不需要持续推进

**单 Session**

> 用户：请用两句话解释 HTTPS 和 HTTP 的区别，给不懂技术的同事看。

预期：Event 进入 Non Project；Radar 安静。

失败条件：创建持续 Item / Project，或建议制定网站安全改造计划。

## 4. 机制守门 Case

### 4.1 新 Query 淘汰旧建议

**Session A**

> 用户：官网四项素材刚刚全部验收，请只列归档文件名，不要扩展其他内容。

预期：完整 QA 结束后可以产生“制定前端开工任务单”Radar，但暂不接受。

随后在同一 Session 立即发送：

> 用户：计划变更，客户暂停官网改版，恢复时间待定。请只记录这次变更。

预期：旧 Radar 变为 `superseded` 并停止展示；Organizer 正常记录暂停状态；新 QA 不应继续建议开工。

失败条件：第二轮输入后旧建议仍可见、可接受或被 Re-entry 恢复。

### 4.2 已授权动作不被新 Query 打断

前置条件：使用正例一生成 Radar，并完成 `pending → accepted → running`，执行尚未结束。

操作：在同一 Session 发起新的真实 Query。

预期：已经获得用户授权的 Radar 继续保持 `running` 并完成，不被转为 `superseded`；新 Query 仍按正常链路处理。

失败条件：已授权动作被新 Query 淘汰、隐藏或重复领取。

### 4.3 Radar 执行不递归触发

前置条件：使用正例一生成的“制定前端开工任务单并排期”Radar。

操作：点击主要动作并等待 AgentV3 执行完成。

预期：原 Radar 依次进入 `accepted → running → done`；执行产物形成 Event 并更新 Item / Project；该 Radar Action Run 不再生成同类或新的 Radar。

失败条件：执行结束后自动出现“继续完善前端任务单”等递归建议，或把 Radar 的 `done` 误当成 Project 完成。

## 5. 记录模板与通过标准

### 5.1 统一运行条件

- 每轮使用全新测试账号，或清空该账号的 Project、Item、Event 和 Radar 业务数据。
- 固定代码版本、主 Agent 模型、Organizer / Radar 模型、提示词和功能开关，并记录测试基准日 `T`。
- 每条消息单独发送，确认主回答结束且 Post-run 进入成功或失败终态后再继续。
- 不复用上一轮的 Item、Project 或 Radar；Re-entry 只用于验证读取，不得用于补触发。

每次运行只记录以下字段：

| 字段 | 内容 |
|---|---|
| Case 与运行时间 | 固定 Case 编号、用户、Session、模型与代码版本 |
| 主 Agent 结果 | 是否覆盖了目标缺口 |
| 状态结果 | Event 归属、Item / Project 状态及关键证据 |
| Radar 结果 | 安静或建议标题、动作、原因、证据、目标 Session |
| 时机结果 | 是否只在完整 QA 后产生，是否正确过期或防递归 |
| 最终判定 | 通过 / 失败，以及唯一主要失败原因 |

每次先判定状态前提：不满足时记为“前置失败 / 本轮无效”，不计入 Radar 通过率，但必须单独统计前置失败率。状态前提满足后，Radar 的出现或安静符合预期并满足第 2 节全部规则，才算单 Case 通过。

评测集通过要求：

- 主动判断组：四个正例至少连续两轮命中正确动作，四个安静反例至少连续两轮保持安静。
- 机制守门组：过期淘汰、已授权动作保护和防递归分别至少连续两轮通过。
- 不允许出现重复主 Agent、阻塞期抢跑、Non Project 误触发、旧建议残留和执行递归。
- 建议措辞可以变化，但目标、动作类型、证据和目标 Session 必须与 Case 一致。
- 同时报告有效运行通过率和前置失败率，不用 Organizer 失败掩盖 Radar 失败，也不反向归责。
