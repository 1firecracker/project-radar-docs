# Project Radar 实施计划

> **执行要求：** 实施者应按任务顺序推进；每个任务完成后运行对应测试并更新本文状态。  
> 文档状态：已批准  
> 负责人：bwz  
> 最近更新：2026-07-10  
> 权威范围：Project Radar 工程任务、先后依赖、进度与完成定义

[返回文档总览](./README.md)

**目标：** 在现有 AgentV3 中完成一条可演示的 Project Radar 端到端闭环。

**实现方式：** 后端在 `end_stream` 后异步扫描并写入 SQLite，前端有限轮询读取结果；接受接口只完成状态转换，随后携带 `radar_item_id` 调用现有 `/api/query` 和 SSE；Context Bundle 作为隐藏运行时指令注入 AgentV3。

**技术栈：** React 18、Vite、FastAPI、Python、SQLite、AgentV3。

## 1. 实施概览

- V0 不新增独立 Dashboard。
- Post-run Scan 是 V0 唯一的 Radar 生成触发器。
- Re-entry 只查询、校验并展示已有建议，不调用扫描服务。
- Session End 不进入 V0 生成链路。
- 同时最多展示一条 Radar Item。
- 不在主 Agent 流式输出、等待用户回答或用户输入时插入 Action Bar。
- 至少一个建议动作必须真实调用 AgentV3。
- 项目归属通过 `session_metadata` 人工标记，Session End 只允许使用演示数据表达。
- Context Bundle 必须在服务端构造。
- V0 Radar API 只支持登录用户。
- Radar Action 发起的 Agent Run 不再触发 Post-run Scan。
- V0 演示配置关闭 next questions，避免与 Radar Scan 并发使用同一 LLM。

### 1.1 当前进度

| 阶段 | 状态 | 交付物 |
|---|---|---|
| 产品定义 | 已完成 | 产品概要、零版产品需求 |
| UI 方案 | 已完成 | 极简主页面 Mock |
| 技术方案 | 已批准 | 技术设计 |
| 后端实现 | 未开始 | 数据表、服务、接口、扫描和执行适配 |
| 前端实现 | 未开始 | Radar 入口、Action Bar、详情和状态交互 |
| 验收测试 | 未开始 | 单元测试、接口测试和演示闭环 |

### 1.2 文件规划

#### 1.2.1 后端

| 文件 | 责任 |
|---|---|
| `backend/database.py` | 创建 Radar 表，提供持久化与条件状态更新 |
| `backend/radar_service.py` | 生成策略、排序、展示过滤、新鲜度和 Context Bundle |
| `backend/server.py` | 注册 Radar API，并在主 Agent 完成后调度扫描 |
| `backend/test_radar_database.py` | 数据持久化、去重和状态转换测试 |
| `backend/test_radar_service.py` | 扫描策略、展示规则和上下文构造测试 |
| `backend/test_radar_api.py` | 查询、接受和反馈接口测试 |
| `backend/test_radar_post_run.py` | Post-run 调度条件、异步失败降级和防递归测试 |
| `agents/agentv3/core/context_pipeline.py` | 注入隐藏 Radar Context Bundle |
| `agents/agentv3/tests/test_context_pipeline.py` | 隐藏 Radar 指令的编译与持久化测试 |

#### 1.2.2 前端

| 文件 | 责任 |
|---|---|
| `frontend/src/App.jsx` | 拉取 Radar、维护状态、接受建议并接入现有消息流程 |
| `frontend/src/components/TodoListPanel.jsx` | 提供右侧 Radar 入口 |
| `frontend/src/components/RadarActionBar.jsx` | 展示输入框上方的单条推进建议 |
| `frontend/src/components/RadarActionBar.css` | Action Bar 样式和状态动画 |
| `frontend/src/components/RadarDetailPanel.jsx` | 展示原因、证据、动作和反馈 |
| `frontend/src/components/RadarDetailPanel.css` | 详情区域样式 |
| `frontend/src/components/radarDisplayState.js` | 集中计算 Action Bar 是否可展示 |
| `frontend/src/components/radarDisplayState.test.js` | 展示条件的纯函数测试 |
| `frontend/src/radarApi.js` | 查询、接受、反馈和 end-stream 有限轮询 |
| `frontend/src/radarApi.test.js` | 轮询停止条件、Re-entry 单次查询和错误降级测试 |

## 2. 后端基础能力

### 2.1 Radar 数据持久化

**依赖：** 无。

**交付：** SQLite 可以保存、查询和更新 Radar Item 与反馈。

- [ ] 先在 `backend/test_radar_database.py` 写入创建、查询、条件状态更新、同键去重和运行关联的失败测试。
- [ ] 在 `backend/database.py` 增加 `radar_items` 和 `radar_feedback` 表初始化。
- [ ] `radar_items` 仅落盘已批准的 V0 字段，包括 `execution_run_id`、`snoozed_until`、`expires_at` 和 `last_error`。
- [ ] JSON 字段限定为 `source_session_ids`、`evidence_refs` 和 `context_snapshot`。
- [ ] 增加按用户、目标会话和状态查询 Item 的函数。
- [ ] 增加创建 Item、写入反馈和更新状态的函数。
- [ ] 状态更新使用“当前状态 + 目标状态”的条件更新，阻止重复接受。
- [ ] 建立 `(user_id, target_session_id, status)`、`(user_id, project_id, dedupe_key, status)` 和 `execution_run_id` 索引。
- [ ] 验证反馈只写入 `radar_feedback`，不嵌入 Item。

验证命令：

```bash
pytest backend/test_radar_database.py -q
```

预期结果：所有 Radar 数据库测试通过。

### 2.2 Radar 服务与扫描策略

**依赖：** 2.1。

**交付：** 给定项目上下文，服务可以生成零条或一条符合约束的 Radar Item。

- [ ] 先在 `backend/test_radar_service.py` 写入结构校验、空结果、错误会话、去重和 Context Bundle 的失败测试。
- [ ] 新建 `backend/radar_service.py`，集中定义字段校验、扫描策略、展示过滤和 Context Bundle。
- [ ] 从 `session_metadata` 读取 `project_id`、`project_role` 和 `project_brief`，只读取当前用户同项目会话。
- [ ] 扫描服务只接受主 Agent 完成后的 Post-run 调用。
- [ ] 使用技术设计中的严格 JSON Schema 调用 LLM；`should_create=false` 或校验失败时返回空结果。
- [ ] 生成结果必须包含原因、至少一条证据、目标会话和建议动作。
- [ ] 同一去重键存在待处理或已完成 Item 时不重复生成。
- [ ] 实现 V0 最小新鲜度检查。
- [ ] 实现 Context Bundle 构造函数。
- [ ] 覆盖 Re-entry 不触发扫描和不生成新 Item。

验证命令：

```bash
pytest backend/test_radar_service.py -q
```

预期结果：扫描策略和 Context Bundle 测试全部通过。

### 2.3 Radar API

**依赖：** 2.1、2.2。

**交付：** 前端可以查询、接受建议和提交反馈；扫描只由后端内部调度。

- [ ] 先在 `backend/test_radar_api.py` 写入未登录、越权、正常查询、重复接受、反馈和不存在 Item 的失败测试。
- [ ] 注册按当前会话查询待处理 Item 的接口。
- [ ] Radar API 使用 `require_auth`，不向共享匿名用户开放。
- [ ] 注册接受建议接口，条件更新 `pending → accepted`，返回 `radar_item_id`、目标会话和短消息，不直接运行 Agent。
- [ ] 注册反馈接口，限制反馈枚举值。
- [ ] 扩展 QueryRequest 接收可选 `radar_item_id`，并验证 Item 所属用户、状态和目标会话。
- [ ] 所有读写操作按当前用户过滤。

验证命令：

```bash
pytest backend/test_radar_api.py -q
```

预期结果：接口成功路径和错误路径全部通过。

## 3. 前端与执行闭环

### 3.1 前端展示骨架

**依赖：** 2.3。

**交付：** 页面能显示右侧 Radar 入口、Action Bar 和详情。

- [ ] 新建 `radarDisplayState.js`，输入 Radar、会话、流式状态、等待回答状态和输入状态，返回是否展示。
- [ ] 先为展示纯函数编写 `radarDisplayState.test.js`。
- [ ] 新建 `RadarActionBar.jsx` 和样式文件。
- [ ] 新建 `RadarDetailPanel.jsx` 和样式文件。
- [ ] 扩展 `TodoListPanel.jsx`，加入 Radar 标签或入口。
- [ ] 新建 `radarApi.js`，封装单次查询和最多六次的 end-stream 有限轮询。
- [ ] 先在 `radarApi.test.js` 覆盖 400ms 首次查询、600ms/1200ms 重试、六次停止、Re-entry 单次查询和错误降级。
- [ ] 在 `App.jsx` 收到 `end_stream` 后启动有限轮询；Re-entry 只执行一次查询。
- [ ] 查询到 Item 后传递给 Action Bar 和右侧详情。
- [ ] 确保无 Radar 时原页面布局和 Todolist 行为保持不变。

验证命令：

```bash
node --test frontend/src/components/radarDisplayState.test.js
node --test frontend/src/radarApi.test.js
npm --prefix frontend run build
```

预期结果：展示规则测试通过，前端构建成功。

### 3.2 接受建议并调用 AgentV3

**依赖：** 2.3、3.1。

**交付：** 点击主要动作后，AgentV3 在正确会话生成真实产物。

- [ ] 用户点击后先调用接受接口，避免前端直接启动重复执行。
- [ ] 接受接口只完成新鲜度检查和 `pending → accepted` 条件更新，返回短消息与 `radar_item_id`。
- [ ] 前端使用返回的短消息和 `radar_item_id` 调用现有 `/api/query`，继续消费原有 SSE。
- [ ] `/api/query` 将 Item 更新为 `running`，写入 `execution_run_id`，并在 `case` 中携带 `radar_item_id`。
- [ ] 服务端从 Item 构造 Context Bundle，以隐藏 `functional_instruction` 注入上下文管线。
- [ ] 在 `agents/agentv3/tests/test_context_pipeline.py` 验证隐藏指令参与 Prompt、不会显示成普通用户消息。
- [ ] Worker 正常 `idle` 结束时更新为 `done`，异常、终止或意外 `waiting_user_input` 时更新为 `failed`。
- [ ] Radar Action Run 结束后跳过 Post-run Scan。
- [ ] 页面刷新后仍能恢复最新 Radar 状态。

验证命令：

```bash
pytest backend/test_radar_service.py backend/test_radar_api.py agents/agentv3/tests/test_context_pipeline.py -q
npm --prefix frontend run build
```

预期结果：后端执行状态测试通过，前端构建成功。

### 3.3 反馈、稍后与忽略

**依赖：** 2.3、3.1。

**交付：** 五类反馈可以保存，稍后和忽略能立即影响展示。

- [ ] 详情区域增加有用、不相关、太频繁、已经完成和上下文不对。
- [ ] 增加稍后与忽略操作。
- [ ] 忽略后更新为 `ignored`；稍后更新为 `snoozed` 并设置 `snoozed_until`。
- [ ] 单次反馈仅写入 `radar_feedback`。
- [ ] 提交成功后立即从 Action Bar 移除当前 Item。
- [ ] 接口失败时保留当前 Item，并显示可重试错误。

验证命令：

```bash
pytest backend/test_radar_database.py backend/test_radar_api.py -q
npm --prefix frontend run build
```

预期结果：反馈与状态测试通过，前端构建成功。

## 4. Post-run Scan 与端到端验收

**依赖：** 2.1 至 3.3。

**交付：** 完整 Demo 可以从正常对话运行到 Radar 完成与反馈。

- [ ] 在主 Agent 完成后的安全边界异步触发扫描。
- [ ] 调度条件限定为登录用户、正常 `idle`、非 stopped、非 waiting_user_input、存在 `project_id` 且不是 Radar Action Run。
- [ ] 扫描失败只记录日志，不影响主回复。
- [ ] 在 `backend/test_radar_post_run.py` 覆盖正常调度、错误降级、停止、等待回答、缺少项目、Radar Run 防递归和 Session End 不生成。
- [ ] Re-entry 时只读取并校验现有待处理 Radar，不调用扫描接口。
- [ ] 确认 Session End 不会在 V0 中创建 Radar Item。
- [ ] 为固定演示会话写入 `project_id`、`project_role` 和 `project_brief`，并准备一条扫描失败兜底 Item。
- [ ] 演示配置关闭 next questions，避免与 Radar Scan 并发调用同一 LLM。
- [ ] 按《测试与演示》运行端到端场景。
- [ ] 运行后端 Radar 测试、相关回归测试和前端构建。
- [ ] 验收通过后更新本文件与 README 的项目状态。

验证命令：

```bash
pytest backend/test_radar_database.py backend/test_radar_service.py backend/test_radar_api.py backend/test_radar_post_run.py agents/agentv3/tests/test_context_pipeline.py -q
node --test frontend/src/components/radarDisplayState.test.js
node --test frontend/src/radarApi.test.js
npm --prefix frontend run build
```

预期结果：Radar 专项测试全部通过，前端构建成功，演示主链路可重复运行。

## 5. 交付标准

### 5.1 交付里程碑

| 里程碑 | 包含任务 | 完成标志 |
|---|---|---|
| 一：可展示 | 2.1 至 3.1 | Post-run 生成的 Radar 可以经有限轮询在正确会话展示 |
| 二：可推进 | 3.2 | accept → query → SSE 链路让 AgentV3 产生真实结果 |
| 三：可闭环 | 3.3 和 4 | 反馈、状态恢复、防递归和完整演示均通过 |

### 5.2 完成定义

- 主链路满足《零版产品需求》的全部 V0 验收标准。
- Radar 专项后端测试通过。
- Post-run 调度、隐藏上下文和 Radar Run 防递归测试通过。
- 前端展示规则与有限轮询测试通过。
- 前端生产构建成功。
- 五分钟 Demo 可以重复执行。
- README、实施计划和测试文档已更新为真实状态。
