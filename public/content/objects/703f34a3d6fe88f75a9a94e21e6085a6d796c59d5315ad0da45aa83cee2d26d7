# Radar 主动判断评测集

> 文档状态：评审中  
> 负责人：bwz  
> 最近更新：2026-07-15  
> 权威范围：Radar 应该提醒与必须安静的固定评测 Case、判定口径和通过标准

[返回文档总览](../README.md)

## 1. 评测目标

本评测集只回答一个问题：**完整 QA 结束并完成状态整理后，Radar 是否发现了用户尚未提出、主 Agent 尚未覆盖、现在又确实值得执行的一步。**

它不把 Organizer 的基础归属准确率计入 Radar 得分，也不把摘要、记忆召回或普通下一步建议算作 Radar 价值。每次运行先验证状态前提：前提不成立记为“前置失败 / 本轮无效”，单独记录 Organizer 问题；只有状态前提成立后，才评价 Radar。

## 2. 统一判定口径

一条 Radar 只有同时满足以下条件才算正确：

1. **新价值**：不是用户本轮明确要求的动作，也不是主 Agent 最终回答已经指出的同一缺口。
2. **当前可推进**：前置条件已满足，动作现在可以由 Agent 执行；仍被阻塞时必须安静。
3. **状态相关**：关联明确的 Item 或 Project，不从 Non Project 或待分类内容生成。
4. **可执行**：动作能产生清单、计划、草稿或总结，不能只有通知、摘要或泛泛提醒。
5. **可解释**：至少一条证据可追溯到 Event，并指向明确的目标 Session。
6. **时机正确**：只在最新完整 QA 的 Post-run 后产生；流式回答、等待用户和 Re-entry 时不新生成。
7. **不过期**：新 Query 淘汰旧的 `pending/snoozed` 建议，但不打断已授权的 `accepted/running` 动作。
8. **不递归**：Radar Action Run 更新状态并产生 Event，但不再生成 Radar。
9. **覆盖来源正确**：只有同一动作被直接交付、直接推荐或用户明确要求时才算覆盖；生成产物内部文字和泛化邀约不算。
10. **约束范围正确**：只检查约束是否命中候选动作，不得把对最终执行的禁止扩大到不同的准备、比较或核对动作。

判定优先级：**先判断是否必须安静，再评价建议内容。** 不该出现却出现的建议，标题再好也算失败。

## 3. 状态与主动判断质量 Case

统一规则：文中的 `T` 是编写占位符，执行前必须替换为实际测试日期，并把 `T+n 天` 换算成具体年月日后再发送，不能把占位符原样输入浏览器。每条消息单独发送并等待主回答结束和 Post-run 进入终态。正例 Radar 均须关联明确 Item / Project、使用最新完整 QA 所在 Session、最多生成一条。若主 Agent 意外指出同一缺口或提出语义相同动作，本轮记为“主回答覆盖 / 正例无效”，Radar 安静是正确行为，不记为漏触发；只有主 Agent 未覆盖时才进入正例判定。用户的窄任务只用于形成自然工作轨迹，不能显式禁止系统给出建议。

### 3.1 状态守门：单一 Item 完成不等于 Project 完成

> 2026-07-15 实测结论：**Item 与 Radar 通过，Project 完成守门失败。** 新 Case 已遵守 V0“一次完整 QA 最多创建或更新一个主要 Item”的边界，不再要求一次建立多个 Item。Session B 后“品牌素材验收”正确完成，主 Agent 也已指出可以进入前端制作，因此 Radar 安静符合去重规则；但官网尚未制作和上线，Project 却错误进入 `completion_candidate`。完整证据见[项目完成守门：单事项复现实测](./案例实测/项目完成守门单事项复现实测.md)。

> 实现进展：同日已完成 `completion_scope`、Organizer `project_patch`、Project 完成守门和统计边界修改；上述文字保留修复前实测结论，当前待按相同输入重跑浏览器。

**Session A**

> 用户：我们计划在 T+16 天前完成星河科技官网改版并正式上线。当前只推进一个事项：品牌素材验收。这个事项的完成标准是 Logo、品牌色规范、产品主视觉和正式宣传文案四项素材全部验收通过。现在 Logo 和品牌色规范已通过；产品主视觉和正式宣传文案由客户承诺在 T+2 天交付。素材验收完成后，项目才能进入前端制作。请帮我记录当前项目和这个事项的状态。

预期：形成一个 Project 和一个“品牌素材验收”Item；Project 与 Item 均为 `in_progress`，Project 完成标准是官网改版完成并正式上线；Radar 安静。

**Session B**

> 用户：继续星河科技官网改版。客户刚确认产品主视觉和正式宣传文案已经交付；连同此前通过的 Logo 和品牌色规范，四项素材现在都已验收无误。请按“星河科技_素材类型_最终版_T”的格式列出四个归档文件名。

预期 Organizer：Event 使用 `event_type=completion_confirmed`、`completion_scope=item`；`item_patch.status=completed`；`project_patch.status=in_progress`，`current_stage` 指向“素材验收完成，待进入前端制作”或等价阶段。

预期状态：仍只有一个“品牌素材验收”Item；该 Item 进入 `completed`。Project 因为官网尚未完成并上线，必须保持 `in_progress`，代码不得覆盖 Organizer 的状态或自动进入 `completion_candidate` / `completed`。

预期 Radar：若主 Agent 已指出可以进入前端制作，Radar 必须安静；只有主 Agent 未覆盖这一推进点时，才允许生成一条与前端开工有关、可立即执行的建议。本 Case 首要评价 Project 完成守门，不计入 Radar 正例命中率。

失败条件：一个 QA 创建多个 Item；素材 Item 没有完成；Project 在未制作、未上线时进入 `completion_candidate` / `completed`；或主 Agent 已覆盖后 Radar 重复提醒。

### 3.2 正例一：分批报价收齐后生成选型表格草稿

本 Case 验证跨 Session 累积事实、最后一个前置条件解除、主回答与 Radar 分工，以及 Radar 定向执行。共使用三个 Session；甲和丙在同一个 Session B，乙在 Session C，不创建 Session D。

**Session A：建立项目背景**

> 用户：我们正在推进客户数据分析平台采购，候选供应商是甲、乙、丙。预算上限为含税 50 万元，本年度不能追加。三家平台的功能、数据规模、集成方式和安全合规评分已经完成，信息安全评审也都已通过。后续需要等三家最终含税报价全部到齐，再交给项目负责人进行选型。请先记录项目状态和评分信息。

预期：形成“客户数据分析平台采购”Project 和当前主要 Item“收集三家最终含税报价并生成选型表格”或等价 Item；Project / Item 保持 `in_progress`。三家报价均未到齐，Radar 安静。Session A 只声明四维评分已经完成，不提供具体数值；后续选型草稿允许把具体评分标为“待确认”，不得补造。

**Session B：收到甲报价**

> 用户：供应商甲发来了最终报价，含税 46 万元。请按照“项目名称_供应商_材料类型_日期”的格式，给这份报价单生成一个归档文件名。

预期主回答：只处理甲报价单归档命名；不得生成选型表格。甲报价写入原 Item，乙、丙报价仍缺失，Radar 安静。

**Session C：收到乙报价**

> 用户：供应商乙的最终含税报价是 48 万元。请帮我起草一句简短的报价接收确认，告诉对方材料已经收到。

预期主回答：只生成简短的接收确认。乙报价跨 Session 写入同一个 Item；丙报价仍缺失，Radar 安静。

**返回 Session B：收到丙报价**

> 用户：供应商丙发来了最终报价，含税 49 万元。请按照之前的归档命名格式，为丙的报价单生成文件名。

预期主回答：只生成丙报价单文件名，可以补充命名假设，但不能直接生成或明确推荐选型表格。

预期 Radar：三家最终含税报价在 Project / Item 层面收齐后，Session B 出现 `type=next_step`、`expected_output=draft` 的“汇总三家报价并生成选型表格草稿”或等价建议，主要动作为“生成选型表格草稿”。草稿汇总甲 46 万元、乙 48 万元、丙 49 万元、预算符合情况、四个评分维度和信息安全评审；未提供的具体评分数值必须标为“待确认”，不得补造或替项目负责人完成最终选型。

目标 Session 要求：Radar 定向到最后一个使报价齐备的 Event 所在 Session B。Session B 同时包含甲、丙两条 Event 是正确行为；Session 只是来源与执行上下文，不要求每条报价单独创建 Session。

动作级判断要求：甲、乙、丙三轮主回答分别完成文件命名或接收确认，与“生成选型表格草稿”不是同一动作；正文中的泛化邀约仍是 `generic_offer`，不构成 `main_answer_covered`。只有主 Agent 已实际生成同一选型表格草稿，或在生成产物之外直接提出同一具体动作时，Radar 才应静默。

就绪判断要求：甲、乙未齐时不得生成下游选型表格；丙报价写入后，报价收集条件解除。若具体评分数值仍未提供，服务端可按无 blocker 可编辑初稿规则生成表格框架并标注待确认字段，不得补造评分，也不因可选信息缺失而直接静默。

跨 Session 新鲜度要求：Session C 和返回后的 Session B 每次更新 Item / Project 都应使依赖旧版本的 `pending/snoozed` Radar 进入 `superseded`；最终 Radar 的 `source_item_version`、`source_project_version` 必须等于丙报价写入后的当前版本。

失败条件：甲或乙报价进入后提前生成选型表格；丙报价未归入同一 Item；要求额外创建 Session D；最终 Radar 错误显示在 Session C；主 Agent 已完成同一动作后 Radar 重复提醒；草稿补造评分；或 Radar 点击后没有形成可编辑表格草稿及 Event 回流。

实测记录：2026-07-16 使用账号 `case32plus` 按上述 Session A → Session B（甲）→ Session C（乙）→ 返回 Session B（丙）路径执行。丙报价写入后，Session B 出现高优先级 `next_step` Radar“汇总三家报价并生成选型表格草稿”，主回答只完成归档文件名，没有覆盖该动作。点击“生成选型表格草稿”后 Agent 生成包含三家报价、预算结论和四维评分待确认字段的草稿，执行结果回流 Item。该三 Session 路径通过，并被确认为当前 3.2 演示基线。

### 3.3 正例二：阻塞逐步解除后主动起草回滚方案

本 Case 验证同一个未完成 Item 的 blocker 跨 Session 逐步解除后，Radar 能重新判断并提出用户本轮没有要求的可执行动作。V0 只追踪一个主要 Item：“完成 CRM 客户门户上线回滚方案”；业务审批、上线窗口和版本基线是该 Item 的 blockers，不得分别创建新的已完成 Item。所有输入和产物均为 Web 对话中的文字，不依赖本地文件、客户端目录或外部办公系统。

**Session A：建立单一 Item 与阻塞关系**

> 用户：我们正在推进 CRM 客户门户 v2.8.3 上线。这个项目当前只需要持续跟踪一个事项：完成上线回滚方案。该方案必须等业务审批通过，并明确生产上线窗口和版本基线后才能定稿。开发、UAT 和上线后 24 小时监控安排已经完成；目前业务审批仍在进行，上线窗口和版本基线也尚未确定。负责人承诺明天反馈，现在不用催办。项目统一要求是：后续回滚方案草稿只能使用已经确认的事实；没有提供的回滚基线、技术步骤、触发阈值和其他角色必须标记“待确认”，不能自行推断。请记录这个事项的状态，并用一句话概括当前进展。

预期主回答：只概括当前进展。

预期状态：建立 CRM 客户门户上线 Project，并且只建立或更新“完成上线回滚方案”这一主要 Item。Item 保持 `in_progress`，blockers 至少包含“业务审批未通过”“上线窗口未确定”“版本基线未确定”。开发、UAT 和 24 小时监控安排必须记录为已完成背景，后续不得退化为待办；“未知信息必须待确认”作为该事项的持续产物约束保留。

预期 Radar：安静。负责人仍在承诺期内，不能催办；回滚方案也尚不可执行。

**Session B：审批解除，但仍缺窗口与版本**

> 用户：继续“完成 CRM 客户门户上线回滚方案”这个事项。业务负责人已经审批通过，但生产团队还没有确定具体上线时间和版本基线。请帮我写一句不超过 35 个字的周报进展。

预期主回答：只输出一句周报进展。

预期状态：Event 必须继续归入原“完成上线回滚方案”Item，不得创建“完成业务审批”新 Item。移除“业务审批未通过” blocker，保留“上线窗口未确定”和“版本基线未确定”；Item 仍为 `in_progress`。

预期 Radar：继续安静。回滚方案仍被两个权威 blocker 阻塞。

**Session C：最后两个 blocker 解除**

> 用户：继续“完成 CRM 客户门户上线回滚方案”这个事项。生产团队已经确认本周六 22:00—23:00 上线，版本基线为 v2.8.3，值班负责人是张明。请帮我写一条不超过 60 个字的内部上线通知。

预期主回答：只生成内部上线通知，不主动起草回滚方案。

预期状态：Event 继续归入原 Item，移除“上线窗口未确定”和“版本基线未确定”两个 blocker。Item 仍为 `in_progress`，因为回滚方案本身尚未完成；不得创建并完成“确认生产窗口与版本”新 Item。

预期 Radar：Session C 出现 `type=risk`、`expected_output=checklist` 或 `draft` 的“起草 v2.8.3 上线回滚方案框架”或等价建议。该动作在本轮之前受阻，现在已经可执行，且主 Agent 的“内部上线通知”没有覆盖它。

点击后预期：在当前 Web 对话中生成文本形式的回滚方案草稿，包括适用版本、上线窗口、值班负责人、回滚触发条件、操作步骤、验证方法和通知范围。服务器地址、命令、数据库操作等未提供信息必须标记“待确认”，不得补造。

目标 Session 要求：Radar 定向最后解除 blocker 的 Session C。Radar 必须绑定仍处于 `in_progress` 的“完成上线回滚方案”Item，不能绑定已经完成的临时 Item。

失败条件：Session A 拆出多个 Item；Session B 或 Session C 新建并完成“审批”或“窗口确认”Item；负责人仍在承诺期内时提前催办；blocker 未解除时提前生成 Radar；24 小时监控安排从已完成退化成待办；Radar 建议监控方案而不是回滚方案；Radar 绑定 completed Item 后立即变为 `ignored`；主 Agent 已生成回滚方案后 Radar 重复提醒；或草稿补造操作信息。

实测记录：2026-07-16 首次使用旧版脚本执行失败。Organizer 将审批与生产窗口拆成两个 completed Item，回滚方案只保留在 Project 摘要中；Radar 随后生成“起草上线后 24 小时监控安排计划”，错误绑定 completed 的窗口确认 Item，并在前端读取时转为 `ignored`。使用单 Item 初稿时，Session A、B 静默和 Session C Radar 均正确，但 Action Runner 补造回滚基线、角色和技术步骤。加入项目级未知字段约束后，补造问题消失，但 Action Runner 丢失 Session B 已确认的“业务审批通过”，重新把它列为待确认；数据库显示该事实没有进入 Radar `context_snapshot`。该结果归类为跨 Session Context Bundle 产品问题，3.3 不继续调词、不修改代码、不重启服务，稳定性状态为“产品问题”。

### 3.4 正例三：接近完成时发现收尾确认缺口

本 Case 验证同一个报告归档 Item 的最后一个 blocker 解除后，Radar 才提出用户尚未要求的收尾产物。所有输入和输出均可在 Web 对话完成。

**Session A：最终 PDF 尚未完成**

> 用户：季度经营报告要在 T+3 天归档。当前只需要持续跟踪“完成季度经营报告归档”这个事项，完成标准包括数据核对、管理层意见处理、最终 PDF、最终核对单和负责人李明签字确认。数据已经核对，管理层意见也处理完毕；最终 PDF 仍在生成，核对单必须等最终 PDF 的文件名和页数确定后才能制作。请先记录当前进展。本轮只记录事项状态，不创建或执行 Todolist，也不要提前推进仍受阻的后续步骤。

预期状态：只形成或更新“完成季度经营报告归档”Item，保持 `in_progress` 或 `waiting`；blocker 为“最终 PDF 尚未生成”。最终核对单和签字属于后续步骤，不得拆成新的 completed Item。

预期 Radar：安静。核对单依赖最终 PDF 的文件名和页数，当前不可执行。

**Session B：最终 PDF 完成，blocker 解除**

> 用户：继续“完成季度经营报告归档”这个事项。最终 PDF 已经生成，共 48 页，文件名确定为“季度经营报告_2026Q2_最终版.pdf”，但整个归档还没有完成。请把“季度经营报告最终 PDF 已经生成，大家可以查阅”润色成一句简短的内部通知，不要写成“已归档”。本次主回答只输出通知正文，不附加解释、提醒或后续建议。

预期主回答：只生成或润色内部通知，不得把报告写成已经归档，不附加核对单或其他后续提醒。

预期状态：Event 继续归入原 Item，解除“最终 PDF 尚未生成” blocker；Item 仍未完成，因为最终核对单和李明签字尚未完成。

预期 Radar：`type=completion` 或 `next_step`、`expected_output=checklist`，生成“制作最终验收核对单与待签字项”或等价动作；证据包含 Session A 的完成标准、最终 PDF 已生成且共 48 页、签字依赖核对单；目标为 Session B。

点击后预期：在 Web 对话中生成最终核对单，列出数据核对、管理层意见处理、最终 PDF 文件名与页数、负责人签字栏；不得把李明尚未签字写成已完成。

失败条件：Session A 提前生成核对单 Radar；一个 QA 拆出多个 Item；主 Agent 把报告写成已经归档；把未签字事项直接标记完成；Session B 主 Agent 已实际生成核对单后 Radar 重复提醒；Radar 只重复内部通知；建议重新制作已经完成的报告内容；或核对单补造未提供的签字时间和验收结果。

实测记录：2026-07-16 首次执行旧版脚本时，Session A 已明确最终 PDF 完成并给出核对单依赖，全部生成条件已经满足，Radar 当轮立即生成“起草最终核对单供负责人签字”。系统行为合理，但与旧 Case 预期“Session B 才提醒”矛盾，归类为 Case 设计问题。第一次 blocker 解除版中，Session A 正确保持安静，但 Session B 的“检查文件名”回答主动提出“需要的话我可以帮你起草核对单”，Radar 因去重规则保持安静；系统行为仍然合理，问题依旧来自 Case。改为润色内部通知后，Radar 主链路和 Action Runner 均符合预期，但主 Agent 擅自把“最终 PDF 已生成”写成“现已归档”。加入事实约束后的第一次运行通过；第二次运行中主 Agent 又直接提醒“可以着手推进核对单”，Radar 正确去重并保持安静。第四版用精确输出约束隔离主回答后，前两次通过，第三次却在 Session A 自动创建 Todolist，并把仍未生成的 PDF、核对单和签字在侧栏显示为 complete。第五版进一步限定 Session A 只记录状态，三次都正确保持 Radar 安静，Session B 三次也都在 blocker 解除后生成核对单 Radar，Item 均保持 `in_progress`；但第三次 Action Runner 把未提供的“页码连续、内容完整、无缺页错页”写为已完成。最终判定：Radar 触发链路稳定，Action Runner 存在不稳定的事实补造产品问题；不改代码、不重启，继续后续 Case。

### 3.5 反例一：主 Agent 已覆盖同一缺口

**单 Session**

> 用户：CRM 客户门户将在 T+6 天上线，开发、UAT 和上线审批已完成，但还没有回滚方案。请审查当前上线风险，明确指出最关键的缺口，并给出可执行的处理建议。

状态前提：形成或更新明确的 CRM 上线 Item / Project，且回滚方案未完成。

主回答观察：必须明确指出缺少回滚方案，并主动提出生成或制定回滚方案；否则本 Case 不具备去重前提，记为本轮无效并重跑。

预期 Radar：安静。

失败条件：Radar 仅更换标题后再次建议生成回滚方案。标题不同但缺口、目标产物和处理动作语义相同，仍属于重复。

实测记录：2026-07-16 使用同一最终脚本在 `radar35_r1_0716`、`radar35_r2_0716`、`radar35_r3_0716` 三个隔离账号中连续执行。三次主回答都明确指出缺少回滚方案并给出具体处理建议，Radar 三次均保持安静。3.5 稳定性结果为 `3 / 3` 通过。

### 3.6 反例二：用户已经要求同一动作

**Session A**

使用 3.1 Session A 建立官网改版 Project 和素材依赖关系。

**Session B**

> 用户：继续星河科技官网改版。四项素材已经全部验收。请制作前端开工任务单框架与日程草案，未知页面和负责人标记待确认。

预期：只有主 Agent 确实产出任务单后才进入判定；Radar 必须安静。

失败条件：Radar 再次建议制定或完善同一份前端任务单。

实测记录：2026-07-16 使用 `radar36_r1_0716`、`radar36_r2_0716`、`radar36_r3_0716` 三个隔离账号执行。三次 Session B 中主 Agent 都实际生成了任务单与日程，Radar 三次均保持安静，核心去重目标 `3 / 3` 通过。但第 2 次 Session A 在客户承诺 7 月 18 日交付、当前为 7 月 16 日时提前生成“起草客户交付跟进消息确认 7/18 按时交付”Radar；其余两次安静。完整 Case 判定为时机判断不稳定的产品问题。

### 3.7 反例三：关键动作仍被阻塞

**Session A**

> 用户：官网前端制作必须等四项品牌素材全部验收后开始。目前 Logo、品牌色和主视觉已通过，正式宣传文案还在客户审核；客户仍在承诺时间内，无需催办，文案确定前也没有 Agent 可执行的准备动作。请记录进展。

**Session B**

> 用户：继续官网改版。请把已经通过的三项素材名称整理成一行，用顿号分隔。

状态前提：Event 归入明确的官网改版 Item / Project，并保留“宣传文案未通过，因此前端仍阻塞”的关系。

预期：主回答只整理三项名称；Radar 安静。

失败条件：出现任何 Radar 均失败，包括建议立即开工、催办或生成当前不可执行的准备材料。

### 3.8 反例四：一次性问答不需要持续推进

**单 Session**

> 用户：请用两句话解释 HTTPS 和 HTTP 的区别，给不懂技术的同事看。

预期：Event 进入 Non Project；Radar 安静。若错误创建 Item / Project，记 Organizer 前置失败；若已正确进入 Non Project 仍产生建议，记 Radar 误触发。

失败条件：创建持续 Item / Project，或建议制定网站安全改造计划。

### 3.9 反例五：事项已完成且没有反例

**Session A**

> 用户：年度客户满意度报告项目的问卷回收、数据分析、报告撰写和部门验收已经全部完成，所有必要事项均验收通过。请把整个项目标记为完成，只确认记录结果。

**Session B**

> 用户：继续年度客户满意度报告。请按“客户满意度报告_归档_实际日期”的格式给出一个归档文件夹名称，不要扩展其他内容。

状态前提：Project 及全部必要 Item 已完成，之后没有缺陷、延期或重开证据。

预期：Radar 安静。

失败条件：建议复盘、继续优化、补计划、再次验收或推进已经完成的事项。

### 3.10 反例六：只有信息价值而无可执行动作

**Session A**

> 用户：法务合同审核正在按计划进行，外部法务将在 T+2 天按约返回意见。目前无需催办，也没有需要提前准备的材料；收到意见后才能决定是否修改合同。请记录当前状态。

**Session B**

> 用户：请把当前状态压缩成一句不超过 30 个字的周报，不要扩展其他内容。

状态前提：事项可正确归属，但下一步完全依赖仍在承诺期内的外部结果，当前没有 Agent 可执行产物。

预期：Radar 安静。

失败条件：展示“项目进展正常”“等待法务反馈”“节点即将到来”等通知、摘要或泛泛提醒。

### 3.11 反例七：动作或执行目标无法唯一确定

测试层级：服务层策略测试，使用固定状态夹具，不依赖 Organizer 随机产生歧义。

状态夹具：同一用户有两个均处于 `in_progress` 的“客户门户上线”Item，分别属于不同 Project 和 Session；最新 Event 只写“审批通过了”，没有项目、客户或 Session 线索，无法可靠确定目标对象和执行位置。

预期：Radar 安静，不得猜测关联 Item、拼凑动作或随意选择 `target_session_id`。

失败条件：生成任何只有概率性归属的建议。

## 4. 机制守门 Case

本组的权威前提均使用固定数据库状态、Radar 记录和可控测试桩，不依赖 Analyzer 随机命中。真实浏览器流程只作为附加端到端验证。

### 4.1 新 Query 淘汰旧建议

前置步骤：固定夹具写入“前端开工”语义的 `pending` Radar，动作统一为“制作前端开工任务单框架与日程草案”，并记录来源 QA 与目标 Session。

随后在同一 Session 立即发送：

> 用户：计划变更，客户暂停官网改版，恢复时间待定。请只记录这次变更。

预期：旧 Radar 变为 `superseded` 并停止展示；Organizer 正常记录暂停状态；新 QA 不应继续建议开工。

失败条件：第二轮输入后旧建议仍可见、可接受或被 Re-entry 恢复。

### 4.2 等待与 Resume 只形成一个完整 QA

测试层级：浏览器观察交互，服务层与数据库断言 Event 数、Analyzer 调用次数和 `source_run_id` 幂等；使用测试桩保证主 Agent 调用 `ask_user` 并进入等待。

前置状态：固定官网改版 Project 已完成开发与验收，但上线日期字段为空。

> 用户：请根据官网改版项目制定上线方案，并先向我确认尚未确定的上线日期。

操作与预期：

1. 主 Agent 进入 `waiting_user_input` 时，不创建 Event、不运行 Organizer，也不生成 Radar。
2. 用户补充“上线日期为 T+6 天，请继续”，Resume 最终进入 `idle` 后才执行一次 Post-run。
3. 最终只产生一条 Event，身份来自原始真实 Query，内容包含最终 Assistant 回答；`resume_answer` 或 `todolist_feedback` 不成为独立 Event。
4. Radar 只允许在最终完整 QA 后判断一次；重复 Resume 不得重复 Event 或 Radar。

失败条件：等待期间出现 Event/Radar、补充回答被记成第二个 Event，或同一 QA 重复触发 Post-run。

### 4.3 Analyzer 迟到结果不得落库

测试层级：服务层集成测试；通过可控测试桩暂停 QA1 Analyzer，浏览器手工操作不作为唯一验收方式。

1. QA1 完成并启动 Analyzer，在模型返回前暂停。
2. 同一 Session 开始 QA2，再释放 QA1 Analyzer。
3. QA1 的 Candidate 不得落库或展示；QA1、QA2 的 Organizer 仍各自正常创建 Event 和更新状态。
4. QA2 仍在 `running` 或 `waiting_user_input` 时，任何 Candidate 都不得提前落库。

失败条件：旧 Candidate 晚到后成为 `pending`、覆盖 QA2，或因新鲜度检查连带跳过 Organizer。

### 4.4 已授权动作不被新 Query 打断

测试层级：服务层集成测试；使用可控屏障分别制造 `accepted` 和 `running` 窗口。

**子 Case A：accepted 保护**

固定夹具写入 `pending` Radar，完成 `pending → accepted` 后在执行领取前暂停，再发起真实 Query。原 Radar 不得变为 `superseded`，随后只能被领取一次。

**子 Case B：running 保护**

使用固定 Radar 夹具确认状态已进入 `running`，再让新的真实 Query 成功进入正常处理链路。原动作继续执行并进入唯一终态，不被淘汰或重复领取。

失败条件：任一已授权状态被新 Query 转为 `superseded`，或同一动作执行多次。

### 4.5 Re-entry 只恢复已有建议

前置步骤：固定夹具写入有效 `pending` Radar，记录 `radar_id`、目标 Session、Event 数和状态版本。

操作：刷新页面，或离开该 Session 后重新进入。

预期：目标 Session 恢复同一个 `radar_id`；切到非目标 Session 不展示，返回目标 Session 仍恢复同一条；不新增 Event、不调用 Organizer / Analyzer、不产生第二条 Radar。再使用 4.1 让它变为 `superseded` 后 Re-entry，不得恢复旧建议。

失败条件：刷新导致重新分析、Radar 复制，或恢复已过期建议。

### 4.6 Radar 执行不递归触发

前置条件：固定夹具写入“制作前端开工任务单框架与日程草案”Radar。

操作：点击主要动作并等待 AgentV3 执行完成。

预期：原 Radar 依次进入 `accepted → running → done`；执行产物形成 Event 并更新 Item / Project；该 Radar Action Run 不再生成同类或新的 Radar。

失败条件：执行结束后自动出现“继续完善前端任务单”等递归建议，或把 Radar 的 `done` 误当成 Project 完成。

### 4.7 整理失败必须阻断 Radar

测试层级：服务层集成测试，使用固定桩让 Organizer 返回无效结构或把 Event 标记为 `classification_failed`。

预期：Event 保留在待分类；不更新 Item / Project；不调用 Analyzer，或至少不允许 Candidate 落库；前端不展示 Radar。此 Case 验证的就是失败守门，因此不记作普通“前置失败 / 本轮无效”。

失败条件：整理失败后仍保存或展示 Radar，或为了生成建议强行归属 Event。

### 4.8 同类建议与冷却期去重

测试层级：服务层策略测试，预置一个同一 Item、同一 `dedupe_key` 的未处理 Radar，或处于有效冷却期的同类历史建议。

操作：再次提交会导向同类动作的有效 Candidate。

预期：保持现有建议，不新增同类 Radar；仅修改标题不构成新建议。

失败条件：同一动作重复落库、重复计数或同时展示。

### 4.9 失效对象和过期时间过滤

测试层级：服务层查询测试，分别预置 `expires_at` 已过期、关联 Item 已完成、关联 Project 已归档的 `pending` Radar。

预期：查询时均不展示或执行旧建议，并按实现规则进入相应终态；其他仍有效的 Radar 不受影响。

失败条件：过期建议或完成/归档对象上的建议仍可见、可接受。

### 4.10 跨 Session 状态版本失效

测试层级：数据库事务、服务层集成和浏览器观察。

1. Session A 为某个 Item 创建一条 `pending` Radar，并记录其 `source_item_version` 与 `source_project_version`。
2. Session B 的新 QA 更新同一 Item，或更新其所属 Project 的语义状态。
3. Organizer 提交新版本的同一事务中，Session A 的旧 `pending/snoozed` Radar 必须进入 `superseded`；`accepted/running` 保持原状态。
4. 如果状态恰好在 Analyzer 运行期间变化，Candidate 落库必须返回 `stale`。
5. 读取或接受遗留旧版本 Radar 时仍须拒绝，不能依赖前端缓存隐藏问题。

失败条件：旧 Radar 在 Session A 继续显示或可接受；Session B 创建结果复用旧版本 Radar；已授权动作被错误中断。

## 5. 记录模板与通过标准

### 5.1 覆盖矩阵

| 产品规则 | 对应 Case | 类型 / 产物 | 测试层级 |
|---|---|---|---|
| 单一 Item 完成后的 Project 完成守门 | 3.1 | Project 保持未完成；Radar 按主回答去重 | 真实浏览器 |
| 分批报价收齐 | 3.2 | `next_step / draft`，生成选型表格草稿 | 真实浏览器；现场主 Case |
| 上线风险控制 | 3.3 | `risk / checklist` | 真实浏览器；现场备选 |
| 接近完成但仍缺确认 | 3.4 | `completion / checklist` | 真实浏览器 |
| 主 Agent 或用户已覆盖 | 3.5、3.6 | 必须安静 | 真实浏览器 |
| 生成产物、泛化邀约与动作级约束 | 3.2、3.5—3.7 | 按覆盖来源和候选动作范围判断 | 真实浏览器 + 服务层策略测试 |
| 阻塞、Non Project、已完成、仅信息价值 | 3.7—3.10 | 必须安静 | 真实浏览器 |
| 目标与执行 Session 不确定 | 3.11 | 必须安静 | 服务层策略测试 |
| 完整 QA、最新 QA 与过期淘汰 | 4.1—4.3 | 生命周期守门 | 服务层集成 + UI 观察 |
| 授权保护、Re-entry、防递归 | 4.4—4.6 | 生命周期守门 | 服务层集成 + UI 观察 |
| 整理失败、同类去重、对象失效 | 4.7—4.9 | 策略守门 | 服务层测试 |
| 跨 Session 状态版本失效 | 4.10 | 旧建议 `superseded`、竞态 `stale` | 数据库事务 + 服务层 + UI 观察 |

### 5.2 统一运行条件

- 每轮使用全新测试账号，或清空该账号的 Project、Item、Event 和 Radar 业务数据。
- 固定代码版本、主 Agent 模型、Organizer / Radar 模型、提示词和功能开关，并记录测试基准日 `T`。
- 每条消息单独发送，确认主回答结束且 Post-run 进入成功或失败终态后再继续。
- 不复用上一轮的 Item、Project 或 Radar；Re-entry 只用于验证读取，不得用于补触发。

每次运行只记录以下字段：

| 字段 | 内容 |
|---|---|
| Case 与运行时间 | 固定 Case 编号、用户、Session、模型与代码版本 |
| 主 Agent 结果 | 未覆盖 / 已覆盖；已覆盖的正例记为“主回答覆盖 / 本轮无效” |
| 状态结果 | Event 归属、Item / Project 状态及关键证据 |
| Radar 结果 | 安静或建议标题、动作、原因、证据、目标 Session |
| 时机结果 | 是否只在完整 QA 后产生，是否正确过期或防递归 |
| 最终判定 | 通过 / 失败、主要失败原因及其他异常 |

每次先判定状态前提：不满足时记为“前置失败 / 本轮无效”，不计入 Radar 通过率，但必须单独统计前置失败率。正例再检查主 Agent 是否覆盖目标缺口；已覆盖记为“主回答覆盖 / 本轮无效”。两项前提均有效后，Radar 的出现或安静符合预期并满足第 2 节全部规则，才算单 Case 通过。

每个质量 Case 最多尝试四次，必须取得至少两次有效运行；两次有效运行都通过才算 Case 通过。四次内不足两次有效运行，记为“可测性失败”，不得从分母中删除。报告必须同时列出总尝试数、有效数、主回答覆盖数、Organizer 前置失败数和 Radar 通过数。

评测集通过要求：

- 主动判断组：三个正例至少连续两轮命中正确动作；状态守门 Case 与所有有效安静反例连续两轮通过。
- 机制守门组：4.1—4.10 使用固定夹具分别至少连续两轮通过。
- 不允许出现重复主 Agent、阻塞期抢跑、Non Project 误触发、旧建议残留和执行递归。
- 建议措辞可以变化，但目标、动作类型、证据和目标 Session 必须与 Case 一致。
- 同时报告有效运行通过率和前置失败率，不用 Organizer 失败掩盖 Radar 失败，也不反向归责。
