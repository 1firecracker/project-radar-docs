# Project Radar 零版产品需求

> 文档状态：已批准  
> 负责人：bwz  
> 最近更新：2026-07-10  
> 权威范围：V0 产品范围、用户流程、交互规则与验收标准

[返回文档总览](./README.md)

## 1. 目标与成功标准

- 展示 AgentV3 可以跨会话理解同一项目。
- 证明主动能力不是通知中心，而是任务边界上的工作推进。
- 完成一条“发现 → 解释 → 行动 → 产出 → 反馈”的闭环。
- 保持现有主页面结构，不新增独立页面。
- 至少一个 Radar 动作真实调用 AgentV3，而不是全链路模拟。

- 五分钟内可以讲清楚 Project Radar 的价值和工作方式。
- 演示可以跑通 Radar 出现、查看原因、点击推进、Agent 产出和用户反馈。
- Radar 不在流式输出中插话，不同时展示多条建议。
- 用户能够理解建议为什么出现，以及依据来自哪里。
- 点击推进后，AgentV3 能基于重新构造的上下文生成真实产物。

## 2. 主链路与触发边界

1. 用户在项目会话中发送消息。
2. 主 Agent 正常完成当前回复或任务。
3. 系统在 Post-run Scan 阶段生成一条高价值 Radar Item。
4. 右侧显示 Radar 入口；强相关时，输入框上方显示 Action Bar。
5. 用户点击“查看依据”，阅读原因和证据。
6. 用户点击“让 Agent 推进”。
7. 系统在正确会话中发起 AgentV3 执行。
8. AgentV3 生成草稿、计划、清单或总结。
9. Radar 状态变为完成。
10. 用户提交有用、不相关、太频繁、已经完成或上下文不对等反馈。

- `Post-run Scan` 是唯一允许生成新 Radar Item 的触发器。
- 每次主 Agent 完成本轮回复或任务后可以触发一次扫描，但扫描结果允许为空。
- 扫描只负责发现用户尚未提出、当前值得关注且可以由 Agent 推进的关键缺口。
- `Re-entry` 只读取、校验并展示已经存在的待处理 Radar，不发起扫描，不生成新 Item。
- `Session End` 不属于 V0 生成链路，留到 V1 实现。

## 3. V0 范围

### 3.1 项目与会话归属

- 预置一个项目标识。
- 手动将两到三个演示会话标记为同一项目。
- 指定一个项目主工作会话。
- Radar Item 明确记录目标会话和推荐原因。

### 3.2 Radar 生成

- 每次主 Agent 完成后最多扫描一次。
- 扫描可以基于演示数据与当前会话上下文混合生成。
- 每次最多形成一条可展示的 Radar Item。
- Radar 必须包含明确的建议动作和至少一条证据。
- 无法形成高价值动作时不生成。

### 3.3 页面展示

- 右侧 Todolist 区域提供“Radar / 1 个推进建议”入口。
- 当前会话与 Radar 目标匹配时，输入框上方显示 Action Bar。
- 详情展示标题、摘要、为什么现在、为什么在当前会话、证据和建议动作。
- 同一时刻最多展示一条最高优先级 Radar。

### 3.4 推进执行

- 点击动作后，聊天区展示一条简短、可理解的用户可见消息。
- 后端构造隐藏的 Context Bundle。
- 接受接口先锁定 Radar，再携带 `radar_item_id` 调用现有 AgentV3 SSE 执行链路。
- 执行期间显示运行状态；成功后标记完成，失败后允许重试。

### 3.5 反馈

V0 支持以下反馈：

- 有用。
- 不相关。
- 太频繁。
- 已经完成。
- 上下文不对。

单次反馈只写入 Radar 反馈记录，不直接进入长期记忆。

### 3.6 允许模拟的能力

- 历史会话自动摘要。
- 自动项目聚类和主会话识别。
- 完整的新鲜度检查。
- 完整的去重和冷却策略。
- 从多次反馈中学习推送偏好。

V0 Demo 可以用预置数据说明未来的 Session End 预整理体验，但不能把它描述为已经实现的 Radar 生成触发器。

## 4. 交互与状态

### 4.1 Action Bar

示例文案：

```text
Project Radar
我发现这个项目已经从“形态调研”进入“V0 范围收敛”阶段。
建议现在锁定第一版范围。
[让 Agent 生成范围] [查看依据] [稍后]
```

展示条件：

- 当前会话属于目标项目。
- Radar Item 与当前会话或项目主会话匹配。
- Radar Item 状态为等待处理。
- Radar Item 有明确建议动作。
- 当前没有更高优先级的待处理 Radar。

不展示条件：

- 主 Agent 正在流式输出。
- 系统正在等待用户回答补充问题。
- 用户正在输入。
- 当前会话与项目无关。
- Radar 只有信息价值，没有下一步动作。
- 同类建议仍处于冷却期。

出现时机：

- 主 Agent 流式输出结束后。
- 输入框为空，用户未处于输入状态。
- 页面仍停留在目标会话。
- 延迟 300 至 800 毫秒淡入。

### 4.2 右侧详情

详情必须包含：

- 标题与摘要。
- 为什么现在需要处理。
- 为什么适合在当前会话推进。
- 支撑判断的会话、记忆、待办或文件证据。
- 一个主要推进动作。
- 稍后、忽略和反馈入口。

默认不常驻展示完整项目快照、全部记忆和多条建议。

### 4.3 状态流

```text
等待处理 → 已接受 → 执行中 → 已完成
等待处理 → 已稍后
等待处理 → 已忽略
执行中 → 执行失败
```

业务含义：

| 状态 | 含义 |
|---|---|
| 等待处理 | Radar 已生成，等待用户处理 |
| 已接受 | 用户接受建议，系统准备执行 |
| 执行中 | Agent 正在生成产物 |
| 已完成 | Agent 已完成目标产物 |
| 已稍后 | 用户暂时不处理 |
| 已忽略 | 用户明确忽略当前建议 |
| 执行失败 | Agent 执行失败，可重新尝试 |

技术字段名称以[技术设计](./技术设计.md)为准。

## 5. 验收标准

### 5.1 发现与展示

- 给定预置项目会话，Post-run Scan 可以产生一条 Radar Item。
- Post-run Scan 没有发现高价值缺口时，不生成 Radar Item。
- Re-entry 只恢复已有待处理 Item，不调用扫描或生成逻辑。
- 非项目会话不显示 Action Bar。
- 流式输出、等待用户回答和用户输入期间不显示新 Radar。
- 页面上同时只出现一条主要建议。

### 5.2 解释

- 用户可以查看为什么现在提醒。
- 用户可以查看为什么适合在当前会话推进。
- 用户可以看到至少一条可追溯证据。

### 5.3 执行

- 点击主要动作后，Radar 依次进入已接受、执行中和已完成状态。
- AgentV3 至少真实生成一份计划、草稿、清单或总结。
- 用户可见消息保持简短，完整上下文不直接显示在聊天区。

### 5.4 反馈

- 五类反馈均可提交并持久化。
- 忽略和稍后会改变当前 Radar 状态。
- 同一条已完成或已忽略 Radar 不会立即再次展示。

## 6. 非目标与后续版本

- 独立 Radar Dashboard。
- 多条 Radar 信息流。
- 对话进行中的实时插话。
- 完整后台 Trigger Engine。
- 外部系统监控和浏览器推送。
- 在归属不明确的会话里直接执行。

V1 增加真实 Session End Scan、多 Radar 管理、自动项目归属、完整反馈学习和新鲜度机制。V2 再考虑计划任务、外部系统变化和多项目持续监控。
